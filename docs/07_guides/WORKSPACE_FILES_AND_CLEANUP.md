# 工作區檔案與分批整理

2026-09-08 root 實際磁碟盤點（排除 `.git`）：66,527個檔案。這不是未提交變更數，執行中的任務可能繼續增加產物。

|位置|檔數|約略容量|用途與處理|
|---|---:|---:|---|
|`.venv/`|32,280|1.30 GiB|Python與安裝套件；保留，不提交|
|`output/`|24,794|7.87 GiB|混合QA、執行狀態、帳本、持久證據；分類後才可清理，不提交|
|`graphify-out/`|3,841|231 MiB|工具衍生資料；先查使用者與活動程序，不直接刪除，不提交|
|`tests/`|2,132|33 MiB|測試及生成快取；保留測試，僅清理可再生快取|
|`scripts/`|810|13 MiB|CLI、排程及快取；不能以數量多就刪除|
|`app_module/`|755|19 MiB|應用服務及快取；保留有效程式|
|`docs/`|454|7 MiB|規格、手冊與驗收；整併需保留權威入口及歷史追溯|

## 現在的處理批次

1. 快取：清單與回滾／再生方法先保存，檢查路徑與使用狀態後，清除可再生的Python快取和明確過期工具暫存。Ops owner執行，root審核。不得刪 `.venv`、`.git`、D原始資料或活動產物。
2. 本機output：按runtime帳本、immutable資料／模型證據、可再生QA、日誌、失敗暫存分類。先確認引用與活動程序，再逐批清理；不可整包刪除，避免破壞自然累積。
3. Git工程變更：每個owner凍結已完成片段，root核對差異／相關測試／文件後按功能提交。未完成或跨owner依賴未驗收者保留，不用全選stage或刪除來製造乾淨工作區。
4. 最終整合：完整QA與排除清單通過後推送，保留main/dev。自然證據仍需持續累積，無須等V4成熟才提交已驗收工程。

## 使用者平常看哪些位置

- 使用入口：`ui_qt/main.py`。
- 操作說明：`docs/07_guides/APPLICATION_MANUAL.md`。
- 目前進度：`docs/00_core/PROJECT_SNAPSHOT.md`。
- 三份新任務：`docs/07_guides/V4_THREE_LUNA_HANDOFF_PROMPTS_2026_09_08.md`。
- 不必逐一閱讀 `.venv`、快取、raw QA 與工具產物。

Commit 是保存原始碼版本；完成後檔案從Git變更列表消失，但仍留在磁碟。清理才會減少磁碟檔案。兩者不能混為一談。
