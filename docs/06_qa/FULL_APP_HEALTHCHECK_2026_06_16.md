# 全程式功能健檢紀錄

> 建立日期：2026-06-16
> 範圍：PySide6 主 UI（`ui_qt/main.py`）目前 8 個頂層工作區與其子分頁 / 主要操作。
> 原則：本檔只記錄人工驗證結果與觀察，不直接提出或執行修復。

## 使用方式

每一項請依實際操作填入：

- 狀態：`待測` / `通過` / `失敗` / `需確認` / `跳過`
- 使用者回覆：記錄實測答案、錯誤訊息、畫面現象或操作結果。
- 證據 / 備註：可填截圖路徑、log 摘要、資料日期、輸出檔名或重現條件。

若遇到失敗，先記錄，不在本檔直接改程式。後續修復任務另開 scope。

## 本輪重點補充（2026-06-16）

本檔是主 UI 人工 smoke test 的單一入口；不要另建平行的 UI 手動驗證清單。專項 QA 報告只記錄特定分頁或特定驗證結論，跨工作區人工走查以本檔為準。

本輪數據更新相關人工驗證請特別確認：

1. 「快速更新（僅 SQLite）」與「安全更新（完整 CSV + SQLite）」會納入 TPEX official daily close quotes。
2. TPEX 日價成功時，CSV 寫入 `DATA_ROOT/daily_price_tpex/YYYYMMDD.csv`，SQLite 寫入 `daily_prices`；TWSE 仍寫入既有 `DATA_ROOT/daily_price/YYYYMMDD.csv` 與同一張 `daily_prices`。
3. TPEX endpoint timeout 或連線失敗時，UI 應以 warning 呈現並繼續其他資料同步，不應靜默失敗，也不應阻斷 TWSE、大盤、產業、券商分點與技術指標流程。
4. `broker_flows` 若仍是舊主鍵，券商分點同步前會先備份 DB，再受控升級為 `(分點名稱, 證券代號, 日期, trade_type)`，讓同一分點 / 股票 / 日期的 `買超` 與 `賣超` 可共存。
5. SQLite 資料檢視的單一日期預設今天，區間預設本月 1 日至今天，日期文字應完整可見；按「清除」後才不套用日期條件。
6. 查詢 `daily_prices` 的 `3207` 時，應可看到 TPEX 歷史資料已補入正式 SQLite；2026-06-17 排查結果為 `20140102..20260617`、共 2,907 筆。若只看到近兩日，代表 TPEX 歷史補齊或 SQLite 同步未完成，需重開數據更新 issue。
7. 表格不應再出現 `PandasTableModel.data 錯誤: The truth value of a Series is ambiguous`；若舊 schema 或 alias 造成重複欄名，UI 必須能正常顯示。

## 本輪重點補充（2026-06-18｜繼續全 UI 健檢）

本輪從 `c785ee3 Improve TPEX refresh workflow` 之後接續。上一輪已完成並 push 的重點：

1. TPEX 日價改走官方歷史查詢 endpoint，手動每日股價、快速更新與安全更新都應以區間流程補缺少 CSV，並同步 `daily_price/` + `daily_price_tpex/` 至 SQLite。
2. 每日股價手動下載完成後會自動同步 SQLite 並執行技術指標增量計算；若 TPEX 失敗，應以 warning 呈現，不應阻斷 TWSE 或後續同步。
3. 每日股價分頁新增「背景補齊 TPEX + 技術指標」與「檢查背景任務狀態」按鈕；背景狀態檔位於 `DATA_ROOT/meta_data/tpex_full_refresh_status.json`，本 repo 內 `meta_data/` 狀態檔與 log 屬本機輸出，不應提交。
4. 主視窗標題已改為 `baldr`，Application Manual 標題已同步。
5. 自動驗證已通過：`tests/test_ui_qt_update_view_workbench.py` 25 passed、`scripts/qa_validate_update_tab.py` 23 passed / 0 failed / 4 skipped、mypy 209 files success、changed Python files `py_compile` 通過。

本輪人工健檢建議順序：

1. 先重測「數據更新」中所有 `已修正待驗證` / `需確認` 項目，尤其 TPEX、SQLite Inspector、背景任務狀態與技術指標。
2. 若數據更新可接受，依序進入「市場觀察」→「每日決策」→「策略回測 / Research Lab」→「推薦分析」→「觀察清單」→「持倉管理」→「Runtime Observatory」。
3. 高風險操作（強制全量、清空、刪除正式紀錄、正式資料重建）只驗證取消 / 防呆 / 提示流程；若要實際執行，另開明確資料任務並先備份。
4. 每個失敗現象先記錄在本檔 Issue 記錄簿，不在本檔直接修程式；修復後狀態只能先改為 `已修正待驗證`，等使用者重新驗證後才改 `驗證通過`。

## 本輪修正摘要（2026-06-23｜Codex）

本輪依目前已記錄 issue 先處理可明確落地的 UI / 文案 / 狀態問題；所有已修項目仍只標示為 `已修正待驗證`，等待使用者重新人工操作後才能改為 `驗證通過`。

已落地修正：

1. 全部資料看板新增「月營收資料」狀態卡，可顯示最新資料年月、筆數與狀態。
2. 市場觀察補 ADX / +DI / -DI 與 0~1 分數說明；Smart Money 主表預設改為 Top / Bottom 50，並支援從右側分點明細雙擊跳到「分點進出追蹤」。
3. Daily Decision warnings 主要 token 改為繁體中文說明，並保留原始 token 供追溯。
4. Research Lab 補最大持倉數 `0=無限制`、允許 `1` 檔、整股限制、Preset 載入回饋、固定門檻 / 百分位排名中文化與 tooltip、推薦回放保存 / 升級按鈕 tooltip、同日收盤成交假設 UI 提醒、快速圖表中文化。
5. 推薦分析補篩選單位 / tooltip、排名方法「最近名次法」、推薦詳情門檻中文化、保存結果成功訊息與「加入觀察清單」文案一致性。
6. 觀察清單手動新增會查正式股票名稱並阻擋不存在代號；「送 Research Lab 批次回測」可直接帶入目前觀察清單。
7. Daily Decision Desk 新增 answer-first dashboard、研究模式註記、市場焦點與股票下鑽；Smart Money 新增 5 / 20 / 60 日語意診斷、quantity-based 集中度與資料品質揭露。

仍需討論或另排大型設計 / 工程項目：

1. 強弱股 / 強弱產業 SQLite-first 與載入效能、券商分點受控並行、技術指標多核心計算。
2. Recommendation Profile 生命週期、自訂 Profile、Research Lab 升級策略是否進推薦分析，以及 regime match / mismatch 規則。
3. Research Lab 大型 UX：最佳化版面與取消流程、推薦回放結果重排、Registry 比較中文化 / 自動刷新、Train-Test / Walk-forward 判讀與資料完整性說明。

## Month 5 基本面資料層補充（2026-06-17）

### 已達成

1. `fundamental_monthly_revenues` 已正式寫入月營收 records：`2014-04..2026-05`、244,499 筆、1,848 檔、0 duplicate；2026-05 為 `observed`，歷史 baseline 為 `degraded`。
2. `fundamental_statement_items` 已正式寫入季度財報 item records：`2014-Q2..2024-Q1`、1,645,555 筆、1,567 檔、0 duplicate；income statement / balance sheet / cash flows 均可在 SQLite Inspector 檢查。
3. `fundamental_valuation_metrics` 已正式寫入 P/E records：831 筆，並保留 industry percentile。
4. `FundamentalSQLiteProvider` 與 `FundamentalFactorService` 已可唯讀產生基本面 factor records / diagnostics；不寫 DB、不接 `ScoringEngine`。
5. Revenue factors 已可產生 YoY、MoM、3M trend、new high。
6. Statement factors 已可產生 EPS、毛利率、營益率、ROE、業外損益 ratio。
7. PB / PS 已有明確 pending diagnostics，不會被 P/E 或 daily price 欄位臨時推導。

### 尚未達成 / 不在 Month 5 接入

1. 基本面 factor 不接 `ScoringEngine`，不影響推薦分數、排名、買賣判斷或回測交易邏輯。
2. PB / PS 尚未正式計算；P/B 仍需 book value per share 或 equity/share count 政策，P/S 仍需 market cap 與 TTM sales 政策。
3. 季度財報目前是 retroactive baseline，quality 為 `degraded`；不得用於導入日前的歷史回測假設。
4. AbnormalFundamentalFlag 目前只具備資料 foundation；是否進風險提示或 scoring 需另開 Month 6/後續政策。

### 新增人工檢查目標

1. SQLite 資料檢視下拉選單應可看到 `fundamental_monthly_revenues`、`fundamental_statement_items`、`fundamental_valuation_metrics`。
2. 查 `fundamental_monthly_revenues` / `2330` 應可看到 `2014-04..2026-05`；`2026-05` 為 `observed`，歷史月份為 `degraded`。
3. 查 `fundamental_statement_items` / `2330` / `2023-Q4` 應可看到 `EPS`、`Revenue`、`GrossProfit`、`OperatingIncome`、`Equity` 等 item。
4. 可執行：

```powershell
.\.venv\Scripts\python.exe scripts\inspect_fundamental_factors.py --all-monthly-revenue-stocks --decision-date 2026-06-30 --diagnostic-limit 12 --stock-summary-limit 12
.\.venv\Scripts\python.exe scripts\inspect_valuation_source_policy.py
```

第一個命令應顯示 `writes_data: false`、`scoring_engine_connected: false`，並列出 revenue / statement / valuation factor counts；第二個命令應顯示 P/E ready、P/B 與 P/S pending。

## 驗證總覽

| 工作區 | 子功能數 | 狀態 | 備註 |
|---|---:|---|---|
| 數據更新 | 29 | 已修正待驗證 | 使用者已完成第一輪人工驗證；長任務切換分頁中斷 / 卡住、SQLite 檢視日期輸入、簡體中文、漲跌欄位、排序需求、TPEX 歷史補齊、背景補齊按鈕、狀態查詢與全部資料月營收狀態卡已修正或新增；Month 5 基本面表與 factor diagnostics 已加入待驗證。 |
| 市場觀察 | 22 | 測試中 | 大盤指數與強 / 弱勢個股、強 / 弱勢產業功能面通過；ADX / DI 與 0~1 分數說明、主力流向 Top/Bottom 顯示、分點 drill-down 已修正待驗證；市場狀態 confidence 計算、強弱排行載入卡頓、趨勢視覺化與中長線籌碼方向呈現仍需後續處理。 |
| 每日決策 | 9 | 測試中 | 頁面可開啟且「更新決策摘要」可載入；warning 工程代碼已補繁中 mapping 待驗證；整體資訊架構與視覺重點不足仍需設計，未來希望整合大盤指數、強弱個股、強弱產業成 dashboard，並可點擊卡片跳轉市場觀察細節。 |
| 策略回測 / Research Lab | 42 | 測試中 | 已完成 B-001 至 B-042 第一輪人工驗證；最大持倉數、Preset 載入回饋、整股限制、推薦回放 tooltip / 保存提示、同日收盤假設 UI warning、策略參數與快速圖表中文化已修正待驗證；批次 / 固定組合差異、推薦回放版面、最佳化 UX / 效能 / 取消、驗證結果判讀、Registry 比較與歷史自動刷新仍需後續設計。 |
| 推薦分析 | 20 | 測試中 | 已完成 R-001 至 R-020 第一輪人工驗證；篩選單位 / tooltip、排名門檻中文化、保存結果導引、加入觀察清單文案與 Research Lab list 命名已修正待驗證；市場狀態資訊、Profile 彈性、新手策略對照、策略傾向偏好與 regime 不匹配規則仍需討論。 |
| 觀察清單 | 13 | 測試中 | 已完成 W-001 至 W-013 第一輪人工驗證；刷新、移除選中、清空候選池、保存選股清單、右鍵移除、選股清單刷新 / 載入 / 新增 / 編輯 / 刪除與跨頁同步功能正常；手動新增股票名稱查找 / 不存在代號阻擋與「送 Research Lab 批次回測」已修正待驗證。 |
| 持倉管理 | 17 | 測試中 | 已完成 P-001 至 P-017 第一輪人工驗證；日記、整理刷新、清空、交易歷史、覆盤日誌、價格監控與籌碼監控主要功能可用。待處理問題包含台股預設手續費 / 證交稅、證券名稱自動查找、摘要卡 UI、交易歷史清除篩選、最新價格日期、來源語意、籌碼風險中文化、主力流向下鑽定位、推薦分析記錄到持倉 Decimal 序列化錯誤，以及 Research Lab 記錄持倉入口不清。 |
| Runtime Observatory | 6 | 測試中 | 已完成 RT-001 至 RT-006 第一輪人工驗證；分頁可載入，FSM / Governance Health / Last Critical Violation / Event Stream 均可顯示，但 Objective、IDLE、HALTED、Rejection Rate、Consecutive Fails、GovernanceViolation 與 event type 多為英文或工程語意，使用者不知道何時觸發與如何判讀，已記錄為後續 issue。 |

## 數據更新

| ID | 分頁 / 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| U-001 | 全部資料 | 檢查數據狀態 | 5 張狀態卡更新最新日期、總筆數與燈號；日誌有檢查結果 | 通過 | 個別功能正常。 |  |
| U-002 | 全部資料 | 快速更新（僅 SQLite） | 下載近期缺失資料並同步 SQLite；進度條與日誌正常；不重寫大型 CSV；TPEX timeout 以 warning 呈現並繼續流程 | 已修正待驗證 | 檢查狀態、清除日誌、頁面說明正常；使用者追問快速更新是否可不更新大型 CSV 只更新 SQLite。 | 快速更新目前仍保留必要日檔 CSV 作為 SQLite 可追溯來源，但跳過 `stock_data_whole.csv` 與券商分點 `merged.csv` 大型重寫；2026-06-18 補充 Manual 說明。 |
| U-003 | 全部資料 | 安全更新（完整 CSV + SQLite） | 依序下載、合併 / 同步 CSV 與 SQLite、計算技術指標；失敗時可讀；TWSE + TPEX 日價同步到 `daily_prices` | 已修正待驗證 | 點更新後，若在尚未更新完成時切到其他 subtab，更新不會繼續跑。 | 已改為保留多個背景 worker，切換 subtab / 狀態查詢不再取消既有長任務；TPEX CSV 寫入 `DATA_ROOT/daily_price_tpex/`，TWSE CSV 保持 `DATA_ROOT/daily_price/`；待使用者重測。 |
| U-004 | 全部資料 | 清除日誌 | 日誌主控台可清空，不影響狀態卡 | 通過 | 清除日誌按鍵正常。 | 2026-06-18 使用者人工驗證。 |
| U-005 | 每日股價 | 日期範圍設定 | 結束日期與最近範圍可調整，切換分頁後日期同步；結束日期可用日曆選取並可一鍵回到今日 | 通過 | 2026-06-18 人工驗證：每日股價 / 大盤 / 產業 / 券商分點的「結束日期」旁已變成清楚的「日曆」按鈕。 | 2026-06-18 修正並驗證：保留跨資料源日期同步；結束日期新增「今日」與「日曆」按鈕，日曆按鈕取代原本不清楚的內建下拉。 |
| U-006 | 每日股價 | 檢查此資料源狀態 | 只更新每日股價狀態與日誌 | 需確認 | 點「檢查此資料源狀態」後下方會顯示完成，但使用者不知道要在哪裡看狀態如何。 | 2026-06-23 人工驗證；狀態結果可見性列 UPDATE-ISSUE-030。 |
| U-007 | 每日股價 | 手動下載此資料源 | 原始資料下載成功；提示需合併才可供分析使用 | 需確認 | 手動下載功能可執行，但日誌顯示 `[2026-06-22 22:44:54] 更新完成：更新完成：成功 1 天，失敗 2 天`；下載後 `2330` 仍沒有 0618 紀錄。 | 2026-06-23 人工驗證；0618 缺漏已列 UPDATE-ISSUE-029，需用修正後重啟的 UI 再測；若重啟後仍失敗，需排查 TWSE 0618 來源下載失敗原因。 |
| U-008 | 每日股價 | 合併每日股價 | 增量合併並同步到 `daily_prices`；確認對話框正常；完成訊息最新日期需與實際合併結果一致 | 已修正待驗證 | 合併 CSV 尚未完成時切到其他 tab，任務會卡住；即使再執行匯出 CSV 且匯出完成，合併仍一直顯示「合併中」。2026-06-18 追加：使用者更正為「所有資料最新在 0618，但合併完成提示仍停在 0617」。 | 合併 worker 不再被切頁狀態查詢或 CSV 匯出覆蓋；完成 / error signal 會釋放各自 worker。2026-06-18 修正：`stock_data_whole.csv` 合併腳本同時納入 `daily_price/` 與 `daily_price_tpex/`，避免 TPEX 已有 0618 但大表只掃 TWSE 目錄而停在 0617。 |
| U-009 | 每日股價 | 匯出 CSV 備案 | 可選最近範圍 / 全部歷史；輸出 UTF-8 BOM CSV | 通過 | 匯出 CSV 可以完成。 | 但在合併卡住期間執行匯出，匯出完成不會解除合併中的錯誤狀態。 |
| U-010 | 每日股價 | 強制重新合併所有每日股價 | 有高風險確認；僅在明確同意後執行；不可破壞原始資料 | 需確認 | 使用者指出強制合併沒有取消按鈕，不理解「測取消流程」是什麼意思。 | 2026-06-23 人工驗證；高風險操作確認 / 取消 UI 列 UPDATE-ISSUE-031。 |
| U-011 | 大盤指數 | 日期範圍設定 | 結束日期與最近範圍可調整並同步 | 通過 | 大盤指數沒問題。 |  |
| U-012 | 大盤指數 | 檢查此資料源狀態 | 顯示大盤資料日期、筆數與日誌 | 通過 | 大盤指數沒問題。 |  |
| U-013 | 大盤指數 | 手動下載此資料源 | 大盤原始資料下載成功 | 通過 | 大盤指數沒問題。 |  |
| U-014 | 大盤指數 | 匯出 CSV 備案 | 匯出 `market_indices` 對應資料 | 通過 | 大盤指數沒問題。 |  |
| U-015 | 產業指數 | 日期範圍設定 | 結束日期與最近範圍可調整並同步 | 通過 | 產業指數沒問題。 |  |
| U-016 | 產業指數 | 檢查此資料源狀態 | 顯示產業資料日期、筆數與日誌 | 通過 | 產業指數沒問題。 |  |
| U-017 | 產業指數 | 手動下載此資料源 | 產業指數原始資料下載成功 | 通過 | 產業指數沒問題。 |  |
| U-018 | 產業指數 | 匯出 CSV 備案 | 匯出 `industry_indices` 對應資料 | 通過 | 產業指數沒問題。 |  |
| U-019 | 券商分點 | 日期範圍設定 | 結束日期與最近範圍可調整並同步 | 通過 | 券商分點日期範圍可以設定。 | 2026-06-23 人工驗證。 |
| U-020 | 券商分點 | 檢查此資料源狀態 | 顯示券商分點資料日期、筆數與日誌 | 需確認 | 點檢查資料源狀態後顯示檢查狀態完成，但使用者不知道狀態顯示在哪裡、該看什麼。 | 2026-06-23 人工驗證；狀態結果可見性列 UPDATE-ISSUE-030。 |
| U-021 | 券商分點 | 手動下載此資料源 | MoneyDJ 分點資料下載成功 | 通過 | 券商分點手動下載功能正常。 | 2026-06-23 人工驗證。 |
| U-022 | 券商分點 | 合併券商分點 | 增量合併並同步到 `broker_flows`；保留 observed / estimated / unavailable 品質 | 已修正待驗證 | 合併券商分點未結束時不能跳其他 tab，否則會卡住。 | 合併 worker 不再被切頁狀態查詢覆蓋；待使用者重測。 |
| U-023 | 技術指標 | 計算技術指標 | 支援全部 / 單一股票、增量 / 全量；結果同步到 `technical_indicators` | 已修正待驗證 | 計算技術指標時出現大量 `[Registry] 舊版配置 ... 使用 legacy 預設值` 與「指標文件缺少日期欄位，將直接合併數據」訊息；單股檔疑似 3010 筆變 6021 筆。 | 2026-06-18 修正：legacy 預設提示每參數只記錄一次，後續降為 debug；技術指標合併會先把 `Date` 正規化為 `日期`，缺少日期時不再直接 concat；並修正同名欄位 canonicalization 誤刪 `日期` / `證券代號` 的問題。 |
| U-024 | SQLite 資料檢視 | 載入、篩選、分頁、Schema | 唯讀查詢、頁碼控制、篩選重設、stale 結果防護、Schema 顯示正常；日期預設空白但打開日曆預設今天；券商分點可由下拉選單選取 | 通過 | 2026-06-18 人工驗證：三個日期欄位預設空白；日期按「日曆」會跳到今天月份；選 `broker_flows` 後，券商分點旁按「展開」可看到下拉清單。 | 已加入日曆日期 picker、繁中欄位 alias、新建 schema 繁中 `漲跌`、`漲跌價差` 依 `漲跌(+/-)` 顯示正負、表頭 server-side ORDER BY 排序；表格以 column position 取值以避免重複欄名造成 Series ambiguity。2026-06-18 修正並驗證：日期預設空白、日曆定位今天月份、單一日期新增「今日」、區間旁單一「清除」會清掉所有日期，`broker_flows` 券商分點改為可輸入可選擇並可展開的下拉。 |
| U-025 | 月營收 | MOPS snapshot 月營收正式資料 | 月營收分頁三個欄位為中文；可先檢查再寫入；正式 DB 已有 `fundamental_monthly_revenues` 244,499 筆 | 已修正待驗證 | Month 5 已正式寫入月營收與歷史 baseline。 | 使用 SQLite Inspector 查 `fundamental_monthly_revenues`，確認 1,848 檔、`2014-04..2026-05`、0 duplicate。 |
| U-026 | SQLite 資料檢視 | 基本面資料表檢視 | 下拉選單可檢視三張 fundamental tables；股票代號與日期篩選可用 | 已修正待驗證 | 之前 SQLite 資料檢視層看不到新的 table。 | 白名單已加入 `fundamental_monthly_revenues`、`fundamental_statement_items`、`fundamental_valuation_metrics`；待人工檢查。 |
| U-027 | 基本面 factor diagnostics | Month 5 factor inspection CLI | 可檢查 revenue / statement / valuation factor records；不寫資料、不接 ScoringEngine | 需確認 | Month 5 基本面不接 scoring，但要能檢查 diagnostics。 | 執行 `scripts\inspect_fundamental_factors.py` 與 `scripts\inspect_valuation_source_policy.py`；確認 PB/PS pending diagnostics。 |
| U-028 | 每日股價 | 背景補齊 TPEX + 技術指標 | 點擊後 UI 不阻塞；不重複啟動第二個背景任務；狀態檔寫入 running / done / failed；不先強制跑 TWSE 全量；技術指標若已與每日股價同日則跳過 | 已修正待驗證 | 背景任務曾先卡在 TWSE 全量，需改為先補 TPEX / SQLite / 技術指標。2026-06-18 追加：狀態顯示 technical running，但資料似乎已存在，懷疑背景任務未先檢查就重算。 | UI 已新增背景按鈕；背景腳本預設不傳 `--twse-update`，TPEX `force_refresh=False`，狀態檔不提交。2026-06-18 修正：移除 UI 背景任務的 `--technical-force-all`，背景腳本先比對 `daily_data.latest_date` 與 `technical_indicators.latest_date`，已最新時直接 skipped；落後時使用 120 天 warmup。 |
| U-029 | 每日股價 | 檢查背景任務狀態 | 可讀取 `tpex_full_refresh_status.json` 並顯示 TWSE / TPEX / SQLite / 技術指標四步狀態；done / failed 後按鈕恢復 | 已修正待驗證 | 需要能確認背景任務是否真的還在跑、成功或失敗。 | 點「檢查背景任務狀態」應在 console 與訊息框顯示狀態、rows 與 message；若無狀態檔，顯示尚未啟動。 |

### 數據更新追加 Issue Record（2026-06-17）

| ID | 來源 | 狀態 | 使用者回覆 / 現象 | 證據 / 備註 |
|---|---|---|---|---|
| U-I-20260617-001 | 每日股價 / TPEX / SQLite | 已修正待驗證 | `3207` 在 SQLite `daily_prices` 只看到最近兩天，懷疑 TPEX 開始有資料但未補歷史。 | 盤點結果：TWSE CSV 已有 `2014-01-02..2026-06-17`；TPEX CSV 原僅 `2026-06-16..2026-06-17`。SQLite 中 `3207` 原為 2 筆；手動補 `2026-06-15` 後為 3 筆。 |
| U-I-20260617-002 | 每日股價 / 手動下載此資料源 | 已修正待驗證 | 選 10 天時沒有明顯跑 TPEX，顯示沒有需要更新。 | 根因：舊 TPEX source 使用 OpenAPI latest endpoint，歷史日期參數未有效回傳指定日期；已改用官方 `/www/zh-tw/afterTrading/otc` JSON 歷史查詢 endpoint，並改為只補缺少 CSV。 |
| U-I-20260617-003 | 全部資料 / 快速更新 | 已修正待驗證 | 快速更新與安全更新差異不清楚，需確認 TWSE + TPEX 是否都跑。 | 快速更新改為 TWSE 每日股價、TPEX 每日股價與券商分點皆只更新結束日前最近 2 天，直接同步 SQLite 並跳過大型合併；安全更新依 UI 日期範圍補齊並執行完整合併。 |
| U-I-20260617-004 | 背景補齊 TPEX + 技術指標 | 已修正待驗證 | 背景流程不應先卡在 TWSE 全量，應先看 SQLite / CSV 缺口再補 TPEX。 | UI 啟動背景腳本時不再傳 `--twse-update`；背景腳本 TPEX 改為 `force_refresh=False`，並以狀態檔回報 TWSE skipped / TPEX / SQLite / technical steps。 |
| U-I-20260617-005 | 每日股價 / SQLite sync | 已修正待驗證 | TPEX CSV 有資料但 SQLite sync 可能因欄位名稱混亂失敗。 | 2026-06-15 TPEX CSV 抓取 879 筆；初次 sync 因 mojibake 欄位判斷失敗，已將 `daily_price_files` sync 正規化到 `日期`、`證券代號`，重跑後該日同步 1,969 筆。 |
| U-I-20260618-001 | Git / QA 母檔接續 | 已記錄 | 接下來要開始繼續全 UI 健檢，需要明確接續點。 | 已補本輪 2026-06-18 重點、建議健檢順序、驗證證據與本機 `meta_data/` 排除提醒。 |

## 市場觀察

| ID | 子分頁 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| M-001 | 大盤指數 | 檢測市場狀態 | Regime、信心度、摘要與策略建議可顯示 | 通過 | 大盤指數顯示正常；檢測市場狀態功能正常。 | 2026-06-19 人工驗證。市場狀態顯示「突破準備」且信心度為 100%，使用者質疑 100% 信心度語意不合理，另列 MARKET-ISSUE-002。 |
| M-002 | 大盤指數 | 技術細節顯示 | 價格 / 均線 / 趨勢 / 評分 / 判斷條件內容合理 | 通過 | 技術細節收合與展開正常。使用者看不懂趨勢指標 ADX / DI 值意義；結構分、強度分、信心度、距離貢獻度皆顯示 1，看起來不直覺，需確認是否合理。 | 2026-06-19 人工驗證。ADX / DI 說明需求列 MARKET-ISSUE-001；結構分數與信心度可解釋性列 MARKET-ISSUE-002。 |
| M-003 | 強勢個股 | 本日 / 本周切換 | 期間切換後資料與標籤一致 | 通過 | 強勢個股功能面沒有問題。 | 2026-06-19 人工驗證；載入卡頓與是否仍讀 CSV 另列 MARKET-ISSUE-003。 |
| M-004 | 強勢個股 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 通過 | 強勢個股內容可顯示，但點下去有明顯卡頓，使用者懷疑仍在讀 CSV，希望評估改用 SQL。 | 2026-06-19 人工驗證；效能 / SQLite-first 評估列 MARKET-ISSUE-003。 |
| M-005 | 強勢個股 | 刷新 | 重新計算 / 重新載入後表格更新 | 通過 | 強勢個股功能面沒有問題。 | 2026-06-19 人工驗證；效能問題另列 MARKET-ISSUE-003。 |
| M-006 | 強勢個股 | 加入觀察清單 | 選取股票可加入候選池，重複加入有合理處理 | 通過 | 選取強勢個股可成功加入觀察清單，且顯示來源。 | 2026-06-19 人工驗證。 |
| M-007 | 弱勢個股 | 本日 / 本周切換 | 期間切換後資料與標籤一致 | 通過 | 弱勢個股功能面沒有問題。 | 2026-06-19 人工驗證；載入卡頓與是否仍讀 CSV 另列 MARKET-ISSUE-003。 |
| M-008 | 弱勢個股 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 通過 | 弱勢個股內容可顯示，但點下去有明顯卡頓，使用者懷疑仍在讀 CSV，希望評估改用 SQL。 | 2026-06-19 人工驗證；效能 / SQLite-first 評估列 MARKET-ISSUE-003。 |
| M-009 | 弱勢個股 | 刷新 | 重新計算 / 重新載入後表格更新 | 通過 | 弱勢個股功能面沒有問題。 | 2026-06-19 人工驗證；效能問題另列 MARKET-ISSUE-003。 |
| M-010 | 弱勢個股 | 加入觀察清單 | 選取股票可加入候選池 | 通過 | 選取弱勢個股可成功加入觀察清單，且顯示來源。 | 2026-06-19 人工驗證。 |
| M-011 | 強勢產業 | 本日 / 本周切換 | 期間切換後資料與排序合理 | 通過 | 強勢產業功能面沒有問題。 | 2026-06-19 人工驗證；載入卡頓與是否仍讀 CSV 另列 MARKET-ISSUE-003。 |
| M-012 | 強勢產業 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 通過 | 強勢產業內容可顯示，但點下去有明顯卡頓，使用者懷疑仍在讀 CSV，希望評估改用 SQL。 | 2026-06-19 人工驗證；效能 / SQLite-first 評估列 MARKET-ISSUE-003。 |
| M-013 | 強勢產業 | 刷新 | 重新計算 / 重新載入後表格更新 | 通過 | 強勢產業功能面沒有問題。 | 2026-06-19 人工驗證；效能問題另列 MARKET-ISSUE-003。 |
| M-014 | 弱勢產業 | 本日 / 本周切換 | 期間切換後資料與排序合理 | 通過 | 弱勢產業功能面沒有問題。 | 2026-06-19 人工驗證；載入卡頓與是否仍讀 CSV 另列 MARKET-ISSUE-003。 |
| M-015 | 弱勢產業 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 通過 | 弱勢產業內容可顯示，但點下去有明顯卡頓，使用者懷疑仍在讀 CSV，希望評估改用 SQL。 | 2026-06-19 人工驗證；效能 / SQLite-first 評估列 MARKET-ISSUE-003。 |
| M-016 | 弱勢產業 | 刷新 | 重新計算 / 重新載入後表格更新 | 通過 | 弱勢產業功能面沒有問題。 | 2026-06-19 人工驗證；效能問題另列 MARKET-ISSUE-003。 |
| M-017 | 主力流向 | 週期切換 | 日線 / 週線 / 月線切換後統計一致 | 已修正待驗證 | 主力流向預設為週線並顯示全部股票；使用者認為不需要一次看所有股票，希望預設或主要視圖改為 Top 50 / Bottom 50。 | 2026-06-23 已新增顯示範圍下拉，預設 Top / Bottom 50；summary 統計仍保留全市場母體，待使用者重測。 |
| M-018 | 主力流向 | 圖表型態切換 | 直方 / 折線 / 面積圖切換正常 | 需確認 | 近期趨勢直方圖會撞到右側看板而被吃掉；直方 / 折線 / 面積圖都不清楚且不美觀。分點進出追蹤也有相同近期趨勢視覺化問題。 | 2026-06-19 人工驗證；視覺化重設計列 MARKET-ISSUE-006。 |
| M-019 | 主力流向 | 開始掃描 | 主表、摘要卡、趨勢圖與右側明細可更新 | 需確認 | 主表與右側買賣超分點明細可顯示，右側分點名稱呈現很有用；但目前只能知道近日買入 / 賣出，無法判斷中長線是否已累積買超 / 賣超、是剛開始買或已買一段時間。 | 2026-06-23 已補 Top/Bottom 顯示；中長線籌碼方向與呈現語意仍列 MARKET-ISSUE-007 待討論。 |
| M-020 | 主力流向 | 表格排序與懸停提示 | 點表頭可排序；近期 5 筆 tooltip 正常 | 需確認 | 近五日 / 近五周的呈現不夠清楚，雖可知道近日買入或賣出，但不容易判讀操作方向與累積階段。 | 2026-06-19 人工驗證；呈現方式改善列 MARKET-ISSUE-007。 |
| M-021 | 主力流向 | 加入觀察清單 | 選取個股可加入候選池 | 通過 | 選取主力流向個股可成功加入觀察清單，且顯示來源。 | 2026-06-19 人工驗證。 |
| M-022 | 主力流向 / 分點進出追蹤 | 選擇券商分點 | 分點操作股票、張數、標籤與趨勢可顯示 | 需確認 | 個股資金流向右側會顯示買賣超分點名稱，但無法雙擊分點後跳轉到分點進出追蹤查看該分點買賣狀況。分點進出追蹤的近期趨勢圖也有不清楚、不美觀問題。 | 2026-06-23 已支援右側分點明細雙擊跳到 Branch Tracker；趨勢視覺化仍列 MARKET-ISSUE-006 待設計。 |

### 市場觀察 Issue 記錄簿

| Issue ID | 來源功能 ID | 問題分類 | 問題摘要 | 目前狀態 | 修正狀態 | 驗證狀態 | 優先級 | 後續處理 |
|---|---|---|---|---|---|---|---|---|
| MARKET-ISSUE-001 | M-002 | UX / 指標說明 | 大盤指數技術細節中的趨勢指標 ADX、DI 值缺少使用者可理解的說明，使用者不知道各值代表什麼。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 已在趨勢指標區補 ADX、+DI、-DI 的繁中說明與 tooltip，強調趨勢強度 / 方向線與非單一買賣訊號限制。 |
| MARKET-ISSUE-002 | M-001 / M-002 | 市場狀態 / 可解釋性 | 市場狀態顯示「突破準備」信心度 100%；技術細節中結構分、強度分、信心度與距離貢獻度皆顯示 1，使用者質疑是否合理且不理解分數語意。 | 部分修正待驗證 | 部分修正 | 計算仍待排查 | 高 | 已補 0~1 規則化分數說明，避免把 1 / 100% 解讀為必然成功；但 confidence 與子分數計算是否合理仍需排查 `RegimeService` / `MarketRegimeDetector`。 |
| MARKET-ISSUE-003 | M-003 / M-004 / M-005 / M-007 / M-008 / M-009 / M-011 / M-012 / M-013 / M-014 / M-015 / M-016 | 效能 / SQLite-first | 強 / 弱勢個股與強 / 弱勢產業功能可用，但進入或載入時有明顯卡頓，使用者懷疑仍在讀 CSV，希望評估改用 SQL。 | 已記錄 | 未修正 | 待排查 | 高 | 盤點 `ScreeningService` / `StockScreener` / `IndustryMapper` 是否仍讀大型 CSV，評估改為 SQLite-first、快取或背景載入；需維持現有排序與欄位語意一致。 |
| MARKET-ISSUE-004 | M-017 / M-019 | 主力流向 / 顯示範圍 | 主力流向預設週線且一次顯示全部股票，資訊量過大；使用者只需要 Top 50 與 Bottom 50。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 主表新增顯示範圍下拉，預設 Top / Bottom 50，並提供 Top 50、Bottom 50、全部切換；summary 統計仍使用全市場掃描結果。 |
| MARKET-ISSUE-005 | M-019 / M-022 | 主力流向 / Drill-down | 個股資金流向右側會顯示買賣超分點，但無法雙擊分點跳到「分點進出追蹤」查看該分點買賣狀況。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 右側分點明細已支援雙擊分點後切換至 Branch Tracker 並選中該分點；若分點不在目前下拉清單，會顯示可讀提示。 |
| MARKET-ISSUE-006 | M-018 / M-022 | 視覺化 / 版面 | 個股資金流向與分點進出追蹤的近期趨勢小圖不清楚、不美觀；第一個直方圖會撞到右側看板而被截掉，折線與面積圖也沒有改善。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | Smart Money 已新增 5 / 20 / 60 日語意診斷欄，降低對短期 sparkline 的依賴；tooltip 揭露 quantity concentration、observed / estimated / unavailable 品質與排除資訊。 |
| MARKET-ISSUE-007 | M-019 / M-020 / M-022 | 主力流向 / 籌碼語意 | 近五日 / 近五周呈現不足以判斷主力是剛開始買入、已買一段時間、正在出貨，或中長線已累積大量買超 / 賣超；操作方向不明朗。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | Smart Money 已新增 `初轉買`、`買超延續`、`初轉賣`、`賣超延續`、`高檔出貨疑慮`、`分點集中異常` 等語意狀態；5 / 20 / 60 日視窗只使用 decision_date 前事件，集中度以 quantity 計算。 |

## 每日決策

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| D-001 | 頂層頁 | 初始化 | 分頁可開啟；若 provider 初始化失敗，有可讀 fallback | 通過 | 每日決策頁面可開啟；目前主要只有「更新決策摘要」按鈕，其他內容由摘要載入後呈現。 | 2026-06-19 人工驗證，附截圖 `codex-clipboard-3f9d22bf-b826-4a62-bb03-04d3a4c6f814.png`。 |
| D-002 | 頂層頁 | 更新決策摘要 | Snapshot 可重建，失敗時不讓整個 App 中斷 | 通過 | 按下「更新決策摘要」後會開始載入並完成頁面更新。 | 2026-06-19 人工驗證。 |
| D-003 | 摘要 | as_of_date / quality / warnings | 日期、整體品質與警告彙總正確顯示 | 已修正待驗證 | 摘要可顯示決策日、生成時間與 degraded 品質；但 warnings 顯示 `relative_strength_liquidity:relative_strength_liquidity_skipped_symbols:1162`、`watchlist_triggers:watchlist_trigger_data_insufficient:2884` 等工程代碼，使用者看不懂，希望改成繁體中文。 | 2026-06-23 已將主要 warning token 轉為繁中說明並保留原 token；待使用者重測。 |
| D-004 | Market Regime | 市場狀態摘要 | Regime label、score、confidence 與品質狀態合理 | 需確認 | 模組狀態區有市場情緒 / 市場狀態摘要，但整體頁面沒有明確視覺重點，使用者不知道最該看什麼。 | 2026-06-19 人工驗證；資訊架構與 dashboard 重設計列 DECISION-ISSUE-002 / DECISION-ISSUE-003。 |
| D-005 | Market Breadth | 市場寬度 | 多方 / 空方 / 持平、新高新低、成交量擴散與 fallback warning | 需確認 | 市況廣度有顯示多方 / 空方 / 持平與廣度比率，但呈現像清單，不像可快速判讀的 dashboard。 | 2026-06-19 人工驗證；資訊架構與 dashboard 重設計列 DECISION-ISSUE-002 / DECISION-ISSUE-003。 |
| D-006 | Sector Rotation | 產業輪動 | 領先 / 落後產業、5 / 20 日變化、輪動強度與 warning | 需確認 | 產業輪動資料可顯示，但頁面總結不夠漂亮且缺少焦點；使用者希望未來能整合強 / 弱勢產業快照，點擊卡片跳轉市場觀察細節。 | 2026-06-19 人工驗證；dashboard 整合列 DECISION-ISSUE-003。 |
| D-007 | Relative Strength / Liquidity | 強弱與流動性 | 強勢、弱勢、低流動性代碼清單與歷史不足降級 | 需確認 | 強弱與流動性資料可顯示，但 degraded / skipped symbols warning 不可讀；使用者希望整合強 / 弱勢個股快照並提供更漂亮的 dashboard。 | 2026-06-19 人工驗證；中文化列 DECISION-ISSUE-001，dashboard 整合列 DECISION-ISSUE-003。 |
| D-008 | Watchlist Trigger | 候選池觸發 | score_bp、risk_alert、新進 / 強度提升 / 下降統計 | 需確認 | Watchlist 提示可顯示，但大量 `watchlist_trigger_data_insufficient` warnings 以工程代碼呈現，使用者看不懂。 | 2026-06-19 人工驗證；中文化列 DECISION-ISSUE-001。 |
| D-009 | Portfolio Alert / Why Not | 持倉警示與風險提示 | condition、chip risk、來源歸因、低流動性 / 弱勢 / watchlist / portfolio 提示 | 需確認 | 持倉警示與 Why Not / 風險提示可顯示，但整體畫面資訊密度高、視覺重點不足，使用者覺得「一堆東西但沒重點」。 | 2026-06-19 人工驗證；資訊架構與視覺重設計列 DECISION-ISSUE-002。 |

### 每日決策 Issue 記錄簿

| Issue ID | 來源功能 ID | 問題分類 | 問題摘要 | 目前狀態 | 修正狀態 | 驗證狀態 | 優先級 | 後續處理 |
|---|---|---|---|---|---|---|---|---|
| DECISION-ISSUE-001 | D-003 / D-007 / D-008 | UX / Warning 中文化 | 每日決策 warnings 直接顯示工程代碼，例如 `relative_strength_liquidity_skipped_symbols`、`watchlist_trigger_data_insufficient`，使用者看不懂。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 已建立主要 warning token 到繁體中文文案的 mapping，說明模組、缺口原因與影響範圍；原始 token 保留在括號中供 debug / 追溯。 |
| DECISION-ISSUE-002 | D-004 / D-005 / D-006 / D-007 / D-009 | UI / 資訊架構 | 每日決策頁面載入後資訊很多但缺少視覺層級與主結論，使用者覺得畫面醜且不知道要看什麼。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | Daily Decision Desk 已新增 answer-first 主結論、研究模式註記、優先 / 風險產業與股票焦點；既有模組細節保留在次層狀態區。 |
| DECISION-ISSUE-003 | D-004 / D-005 / D-006 / D-007 | 產品設計 / 市場觀察整合 | 使用者希望未來把每日決策與市場觀察整合，形成漂亮 dashboard：顯示大盤指數、強弱個股、強弱產業快照，並能點擊卡片跳轉到市場觀察 tab 看細節。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | Daily Decision Desk 已新增市場焦點區與股票下鑽按鈕；點擊股票焦點會切到「市場觀察 > 主力流向」並定位該股票。 |

## 策略回測 / Research Lab

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| B-001 | 實驗模式 | 單股回測模式 | 模式切換後欄位、按鈕與說明一致 | 通過 | 單股回測模式切換正常。 | 2026-06-19 人工驗證。 |
| B-002 | 實驗模式 | 批次股票回測模式 | 可從選股清單載入多檔並進入批次結果 | 通過 | 批次股票回測模式功能正常；但「最大持有部位數」語意不清，使用者詢問是否可選無限制到 50。 | 2026-06-19 人工驗證；見 BACKTEST-ISSUE-001。 |
| B-003 | 實驗模式 | 固定組合回測模式 | 固定組合 metadata 與 per-stock 保存提示一致 | 通過 | 固定組合回測模式功能正常；但與批次股票回測的差異不清楚，使用者只明顯注意到策略回測有啟用多核心選項。 | 2026-06-19 人工驗證；見 BACKTEST-ISSUE-002。 |
| B-004 | 實驗模式 | 推薦系統回放模式 | 推薦回放設定區可用；執行入口正常 | 需確認 | 功能看起來正常，但寬度太寬，右側內容疑似被吃掉一點。 | 2026-06-19 人工驗證；見 BACKTEST-ISSUE-003。 |
| B-005 | 實驗模式 | 策略研究模式 | 策略研究相關 UI 顯示正常 | 通過 | 策略研究模式顯示正常。 | 2026-06-22 人工驗證；資訊架構可再改善，見 BACKTEST-ISSUE-004。 |
| B-006 | 策略來源 / 預設 | 儲存 Preset | 可保存目前策略 / 參數設定 | 通過 | 儲存 Preset 功能正常。 | 2026-06-19 人工驗證。 |
| B-007 | 策略來源 / 預設 | 載入 Preset | 載入後 UI 控制項與策略設定同步 | 已修正待驗證 | 使用者確認載入 Preset 按下按鈕後沒有任何變動；選擇下拉的 `-- 選擇預設 --` 也沒有跳回預設或提示。 | 2026-06-23 已補空選項提示、服務缺失提示與成功套用摘要；待使用者重測。 |
| B-008 | 策略來源 / 預設 | 刪除 Preset | 有確認流程；刪除後清單更新 | 通過 | 刪除 Preset 功能正常。 | 2026-06-22 人工驗證。 |
| B-009 | 輸入來源 | 單一股票 | 股票代號輸入與日期範圍可用 | 通過 | 單一股票輸入功能正常；使用者希望開始 / 結束日期加入日曆選項，開始日期預設一年前且日曆打開也定位一年前，結束日期預設今天且日曆打開也定位今天。 | 2026-06-22 人工驗證；日期 UX 需求見 BACKTEST-ISSUE-005。 |
| B-010 | 輸入來源 | 選股清單 | 下拉清單可載入 Universe；管理按鈕可開啟清單管理 | 通過 | 選股清單功能正常。 | 2026-06-22 人工驗證。 |
| B-011 | 選股清單管理 | 新增 / 編輯 / 刪除 / 關閉 | 對話框操作與資料同步正常 | 通過 | 選股清單管理功能正常。 | 2026-06-22 人工驗證。 |
| B-012 | 資金成本 / 執行 | 初始資金 | 範圍、格式、整股限制提示合理 | 已修正待驗證 | 使用者詢問「整股限制提示是什麼？」顯示目前初始資金區對台股 1000 股整股限制與資金不足造成 0 交易的提示不夠清楚。 | 2026-06-23 已補初始資金 tooltip，說明台股 1000 股整股、向下取整與資金不足可能造成 0 交易；待使用者重測。 |
| B-013 | 資金成本 / 執行 | 手續費 bps / 滑價 bps | 可輸入並進入回測 metadata / 報告 | 通過 | 手續費 bps / 滑價 bps 功能正常。 | 2026-06-22 人工驗證。 |
| B-014 | 資金成本 / 執行 | 成交價假設 | `next_open` / `close` 切換，結果揭露一致 | 通過 | 成交價假設切換功能正常。 | 2026-06-22 人工驗證。 |
| B-015 | 停損停利 | 百分比模式 | 停損 / 停利百分比生效 | 通過 | 停損停利百分比模式功能正常。 | 2026-06-22 人工驗證。 |
| B-016 | 停損停利 | ATR 倍數模式 | ATR 停損 / 停利倍數生效 | 通過 | ATR 倍數模式功能正常。 | 2026-06-22 人工驗證。 |
| B-017 | 部位 Sizing | 全倉 | 部位計算符合整股與資金限制 | 通過 | 全倉 sizing 切換正常。 | 2026-06-22 人工驗證；整股限制說明不足另列 BACKTEST-ISSUE-006。 |
| B-018 | 部位 Sizing | 固定金額 | 每次固定金額設定生效 | 通過 | 固定金額 sizing 切換正常。 | 2026-06-22 人工驗證。 |
| B-019 | 部位 Sizing | 風險百分比 | 風險比例與 ATR 互動正確 | 通過 | 風險百分比 sizing 切換正常。 | 2026-06-22 人工驗證。 |
| B-020 | 部位管理 | 最大持倉數 | 批次 / 組合模式下限制生效 | 已修正待驗證 | 最大持倉數預設為「無限制」、上限 50，但下一個可選值是 2；使用者詢問最小只能 2 是否正常。 | 2026-06-23 已改為 0=無限制、1=最多 1 檔、上限 50，並補 tooltip；待使用者重測。 |
| B-021 | 部位管理 | 等權 / 分數加權 / 波動調整 | 權重模式切換不報錯，metadata 記錄正確 | 通過 | 等權 / 分數加權 / 波動調整切換正常。 | 2026-06-22 人工驗證。 |
| B-022 | 部位管理 | 允許加碼 | 勾選後加碼邏輯與結果可辨識 | 通過 | 允許加碼切換正常。 | 2026-06-22 人工驗證。 |
| B-023 | 部位管理 | 允許重新進場 / 冷卻期 | 冷卻期與重進場限制生效 | 通過 | 允許重新進場 / 冷卻期切換正常。 | 2026-06-22 人工驗證。 |
| B-024 | 市場限制 | 漲跌停限制 | 限制開關影響成交與未成交原因 | 通過 | 漲跌停限制功能正常。 | 2026-06-22 人工驗證。 |
| B-025 | 市場限制 | 成交量約束 / 最大參與率 | 低流動性或參與率超限時有合理拒絕 / 標記 | 通過 | 成交量約束 / 最大參與率功能正常。 | 2026-06-22 人工驗證。 |
| B-026 | 策略配置 | 策略下拉與參數 | 策略切換後參數與說明更新 | 已修正待驗證 | 三種策略未全部顯示繁體中文，其中 `baseline score threshold` 顯示英文；切到爆衝策略與穩健策略時，門檻模式欄位顯示 `Threshold mode`，選項 `fixed` / `quantile` 也未中文化，hover 無說明。 | 2026-06-23 已中文化 baseline 策略名稱、門檻模式 label、固定門檻 / 百分位排名選項與 tooltip；待使用者重測。 |
| B-027 | 進階驗證 | 參數最佳化 | 可執行參數掃描，目標包含 Sharpe、年化報酬、CAGR-MDD | 需確認 | 功能可啟動，按「執行最佳化 / 參數掃描」後會真的執行並顯示參數組合；但範圍欄位展開後左側面板寬度不足需水平滑動或調整視窗。2330 測試時產生 8 萬多組參數，執行很久；使用者詢問是否可用 8 threads / 多核心並行、目前讀 CSV 或 SQL。取消後 UI 顯示需等待子任務結束才解鎖，terminal 持續列印大量 `最佳化子任務 xxxx 異常:` 空訊息；使用者最後已關閉 UI。 | 2026-06-22 人工驗證；版面列 BACKTEST-ISSUE-010，效能 / 並行列 BACKTEST-ISSUE-011，取消與錯誤訊息列 BACKTEST-ISSUE-012。 |
| B-028 | 進階驗證 | 套用選中參數 | 最佳化結果可套回設定 | 通過 | 套用選中參數功能正常。 | 2026-06-22 人工驗證。 |
| B-029 | 進階驗證 | Train-Test Split | 可執行驗證並顯示 OOS 結果 | 需確認 | Train-Test Split 可快速跑出結果；使用者以預設日期（去年到今天）、單檔 2330 測試。訓練集總報酬 56.57%、年化 90.54%、Sharpe 2.50、MDD -8.37%、勝率 100%；測試集總報酬 10.28%、年化 41.00%、Sharpe 1.45、MDD -77.29%、勝率 100%；夏普退化 -41.79%，顯示策略穩定性一般。使用者不確定結果是否正常。 | 2026-06-22 人工驗證；結果判讀與異常指標可解釋性列 BACKTEST-ISSUE-013。 |
| B-030 | 進階驗證 | Walk-forward | 訓練月數、測試月數、步進月份生效 | 需確認 | Walk-forward 可快速跑出結果；使用者以預設日期（去年到今天）、單檔 2330 測試。總 Fold 數 2、平均訓練 Sharpe 1.75、平均測試 Sharpe 2.20、平均退化 27.2%、一致性 100.0%。使用者詢問單檔一年資料幾乎沒有計算時間是否正常。 | 2026-06-22 人工驗證；2 folds 樣本不足與結果解讀提示列 BACKTEST-ISSUE-013。 |
| B-031 | 推薦回放 | Top N / 候選上限 / 持有天數 | 推薦回放參數進入執行快照 | 通過 | 推薦回放 Top N / 候選上限 / 持有天數可正常顯示。 | 2026-06-22 人工驗證。 |
| B-032 | 推薦回放 | 重播頻率 / 資金配置 | 每週重播 / 只跑一次、等權 / 分數加權正常 | 已修正待驗證 | 重播頻率 / 資金配置可正常切換；但滑鼠 hover 沒有顯示說明。 | 2026-06-23 已補每週重播 / 只跑一次、等權 / 分數加權 tooltip；待使用者重測。 |
| B-033 | 執行與下一步 | 執行實驗 | 背景執行、進度、按鈕鎖定與完成狀態正常 | 需確認 | 推薦回放可從推薦分析送入 Research Lab 後執行並跑出結果；terminal 顯示 `推薦組合回測目前採用「同日收盤訊號同日收盤成交」之理想化研究假設` warning。結果摘要顯示總報酬 53.72%、MDD -21.37%、交易檔數 291、平均持有 3.8 天、資金使用 488,333,333、虧損交易占比 54.0%、最拖累股票 3580 友威科、Sharpe 1.66、Sortino 1.73、Monte Carlo P05/P50/P95 皆 795.73%；摘要內容重複出現。期間明細、個股貢獻與交易紀錄被底部吃掉看不到。 | 2026-06-23 已把同日收盤成交假設與 gap / liquidity / cash / lot-size 風險加入 UI 摘要；結果頁重排與數值語意仍列 BACKTEST-ISSUE-015 待設計。 |
| B-034 | 執行與下一步 | 取消執行 | 合作式取消後 UI 解鎖，不產生新舊任務重疊 | 通過 | 一般任務取消正常，可順利取消並解鎖 UI。 | 2026-06-22 人工驗證；大型最佳化取消問題另見 BACKTEST-ISSUE-012。 |
| B-035 | 執行與下一步 | 保存結果 | 成功結果可保存到 Research Run Registry；stale result 不可保存 | 已修正待驗證 | 推薦回放成功跑出結果後，「保存結果」仍點不下去；使用者不知道什麼條件才能觸發保存。 | 2026-06-23 已確認成功結果會啟用保存按鈕並補 tooltip / 成功訊息；待使用者重測。 |
| B-036 | 執行與下一步 | 升級為策略版本 | Registry Gate、最低樣本與完整性限制生效 | 已修正待驗證 | 「升級為策略版本」點了沒有反應；使用者不知道是否因未保存、Gate 未通過或功能失效。 | 2026-06-23 已補單股與推薦回放保存 / 升級按鈕 tooltip，說明未保存或 gate 條件不足時的原因；更完整 gate result UI 仍可後續加強。 |
| B-037 | 結果 / 實驗摘要 | 匯出 Excel 報告 | 單股成功後啟用；背景原子寫入；資料完整性區塊正確 | 通過 | 單檔股票可匯出「單股回測研究報告」Excel；報告包含 Traceability、Data Integrity、Parameters、Metrics、Validation。使用者提供匯出內容，資料完整性警示顯示缺失欄位 `data_as_of_date`, `data_version`, `regime`, `benchmark`。 | 2026-06-22 人工驗證；缺失欄位可解釋性列 BACKTEST-ISSUE-019。 |
| B-038 | 結果 / 圖表 | 權益、回撤、報酬分佈、持有天數 | fast renderer / fallback 顯示正常 | 部分修正待驗證 | 權益 / 回撤 / 報酬分布 / 持有天數圖可顯示，但報酬分布與持有天數圖仍有英文標題與標籤，使用者看不懂如何判讀，也不確定目前呈現是否正常或是否有更好方式。另使用者補充：圖表區要先跑過任何一種測試後，才會出現以前的策略圖表，舊圖表沒有在進入 tab 時自動載入。 | 2026-06-23 已將 fast renderer 報酬分布與持有天數圖標題、軸、legend、marker 中文化，待重測；但既有策略圖表自動載入尚未完整修正，需更多修改，列 BACKTEST-ISSUE-021。 |
| B-039 | 結果 / 歷史與比較 | 重新整理 / 刪除選中 / 比較選中 | legacy run 操作正常，有確認與錯誤處理 | 需更多修改 | 歷史與比較 tab 需要手動點「重新整理」才讀取既有紀錄；使用者點進去不知道是否有紀錄，覺得手動刷新很煩。 | 2026-06-23 程式碼核對：`_refresh_history()` 已存在，但目前主要由按鈕 / 保存後呼叫，未見初始化或 tab 進入時自動 refresh；列 BACKTEST-ISSUE-021。 |
| B-040 | 結果 / 批次結果 | 排行榜、雙擊明細、匯出批次 Excel | 批次結果可讀、雙擊載入明細、匯出成功 | 需確認 | 批次結果可以比較，但使用者不清楚比較的意義，也不知道應該比出什麼東西。 | 2026-06-22 人工驗證；比較目的與判讀說明列 BACKTEST-ISSUE-022。 |
| B-041 | 結果 / 推薦回放 | 保存 / 刪除 / 升級 / 匯出回放 Excel | 回放結果、credibility、unfilled、cash、weight、gap 顯示與操作正常 | 需確認 | 推薦回放已可成功保存並成功升級，但使用者不清楚「升級成策略版本」代表什麼，也不知道升級後去哪裡看。 | 2026-06-22 人工驗證；升級後導引列 BACKTEST-ISSUE-023；結果頁呈現另見 BACKTEST-ISSUE-015 / BACKTEST-ISSUE-016。 |
| B-042 | 結果 / Registry 比較 | 篩選、分頁、選取 2-5 run、比較 | comparability badge、參數差異、normalized equity、metrics 與 benchmark 顯示 | 需更多修改 | Registry 比較可顯示與比較，但非常不直觀。`run type` 不易理解，選項如 `single_backtest`、`recommendation_portfolio` 需 UI 中文化；參數差異、Metrics、Regime、Benchmark 內容多為英文且表格被吃掉；Normalized Equity 空白且不知道如何使用；Research Runs 區塊與下方區塊間出現白色框線不美觀。使用者希望 UI 顯示盡量中文化，存檔內容可維持英文。Registry 比較與歷史與比較 tab 都需手動刷新才讀取既有紀錄。 | 2026-06-23 程式碼核對：`RunRegistryCompareWidget.refresh_runs()` 已存在，篩選與按鈕會呼叫，但初始化 / tab 進入時未見自動 refresh；Registry 中文化 / 排版 / 自動載入列 BACKTEST-ISSUE-024 / BACKTEST-ISSUE-021。 |

### Research Lab Issue 記錄簿

| Issue ID | 關聯項目 | 嚴重度 | 狀態 | 使用者觀察 | 初步處理方向 |
|---|---|---|---|---|---|
| BACKTEST-ISSUE-001 | B-002 / B-020 | 中 | 已修正待驗證 | 「最大持有部位數」語意不清；畫面可選「無限制」到 50，但使用者不知道它代表什麼。 | 已補 hover / 說明文字，並確認 UI 使用 0 代表無限制、1 代表最多 1 檔、上限 50；待使用者重測。 |
| BACKTEST-ISSUE-002 | B-003 | 中 | 新增 | 批次股票回測與固定組合回測差異不清楚；使用者只明顯看到多核心選項差異。 | 已修正待驗證：Research Lab 模式提示已改為「適合 / 輸入來源」格式，說明單股、批次、固定組合、推薦回放與策略研究的使用情境。 |
| BACKTEST-ISSUE-003 | B-004 / B-031 / B-032 / B-041 | 中 | 新增 | 推薦系統回放功能看起來正常，但設定區寬度太寬，右側被吃掉一點。 | 檢查 Research Lab 右側面板、scroll area 與推薦回放設定區的最小寬度 / resize policy，避免內容超出可視範圍。 |
| BACKTEST-ISSUE-004 | B-005 / B-027 / B-030 | 中 | 新增 | 策略研究模式不知道應該看什麼，也不清楚它和一般回測的差異。 | 補強策略研究模式的資訊架構：明確顯示策略來源、輸入來源、參數最佳化與 Walk-forward 驗證區塊，並用繁中說明此模式用於比較策略模板、參數與驗證結果。 |
| BACKTEST-ISSUE-005 | B-009 | 中 | 新增 | 單一股票模式功能正常，但日期輸入希望加入日曆選項；開始日期預設一年前，點開日曆也定位一年前；結束日期預設今天，點開日曆也定位今天。 | 已修正待驗證：開始 / 結束日期已使用 calendar popup，開始日期預設 `today - 1 year`，結束日期預設 `today`，並加入 UI 測試覆蓋。 |
| BACKTEST-ISSUE-006 | B-012 / B-017 | 中 | 已修正待驗證 | 使用者不清楚「整股限制提示」代表什麼；初始資金不足時為何可能沒有交易不直覺。 | 已在初始資金 tooltip 補明確提示：台股整股以 1000 股為一張，買進會向下取整為 1000 股倍數；若初始資金或單筆配置不足以買一張，回測可能拒絕交易或出現 0 交易。 |
| BACKTEST-ISSUE-007 | B-020 | 中 | 已修正待驗證 | 最大持倉數預設顯示「無限制」，上限 50，但最小實際可選值變成 2；使用者不確定是否正常。 | `max_positions_input` 已改為最小 0、預設 0、special text「無限制」，並允許設定 1 檔持倉；待使用者重測。 |
| BACKTEST-ISSUE-008 | B-006 / B-007 | 高 | 已修正待驗證 | Preset 儲存正常，但載入時按下按鈕完全沒有任何變動；選到 `-- 選擇預設 --` 也沒有跳回預設或提示；使用者也不清楚 Preset 保存範圍是否包含股票代號、初始資金、手續費等執行設定。 | 已修正載入按鈕與空選項處理；選到空項目會提示，成功載入會顯示策略 / Preset / 參數數量摘要。Preset 保存範圍是否要擴成完整實驗模板仍可後續討論。 |
| BACKTEST-ISSUE-009 | B-026 | 中 | 已修正待驗證 | 策略下拉與策略參數中文化不完整：`baseline score threshold`、`Threshold mode`、`fixed`、`quantile` 仍顯示英文；fixed / quantile hover 沒有說明差異。 | 已統一 Research Lab 策略顯示名稱與參數 label：baseline 策略改繁中名、`threshold_mode` 改「門檻模式」、`fixed` / `quantile` 顯示「固定門檻 / 百分位排名」，並補 tooltip 說明資料時點、60 日暖機與限制。 |
| BACKTEST-ISSUE-010 | B-027 | 中 | 新增 | 參數最佳化欄位從固定值切成範圍時，左側面板寬度不足，部分資訊看不見，需要水平滑動或手動調整視窗。 | 調整 Research Lab splitter / scroll area / optimization row layout；參數最佳化區塊在展開範圍欄位時應可自動取得更寬空間，或改成兩列式/表格式配置，避免依賴水平捲動。 |
| BACKTEST-ISSUE-011 | B-027 | 高 | 新增 | 單股最佳化可產生 8 萬多組參數，執行時間極長；使用者希望能用 8 threads 或多核心並行，並想知道目前讀 CSV 或 SQL。 | 盤點 OptimizerService：目前已預載單股資料一次，`config.use_sqlite=True` 時優先讀 SQLite，缺資料才 fallback CSV；目前使用 ThreadPoolExecutor，預設最多 8 workers。需評估改為 ProcessPool / vectorized / early pruning / 組合數預估與警告 / 使用者可設定 worker 數，並在執行前顯示預估組合數與耗時風險。 |
| BACKTEST-ISSUE-012 | B-027 / B-034 | 高 | 新增 | 取消 8 萬多組參數掃描後，UI 顯示需等待子任務結束才解鎖，terminal 持續大量輸出 `最佳化子任務 xxxx 異常:` 空訊息；使用者不確定是否可以關閉 UI，最後已關閉 UI 結束本次檢查。 | 改善最佳化取消流程：取消後停止送出/處理剩餘 futures、不要對已取消 futures 逐筆輸出空錯誤；將 cancellation 當正常狀態彙總顯示。UI 應提供「已送出取消，剩餘 N 個子任務清理中」與可預期解鎖狀態；必要時提供安全終止提示。 |
| BACKTEST-ISSUE-013 | B-029 / B-030 | 中 | 新增 | Train-Test / Walk-forward 能跑出結果，但使用者不確定該如何判讀；單檔 2330、約一年資料下很快完成。Train-Test 測試集出現總報酬 10.28%、勝率 100%、最大回撤 -77.29% 的不直覺組合；Walk-forward 只有 2 folds 卻顯示一致性 100%。 | 補強驗證結果說明與可靠度提示：單檔短期間快速完成屬正常，但 fold 數 < 3 或交易次數不足時應顯示「樣本不足，不宜作正式策略判斷」；檢查測試集最大回撤計算與呈現是否合理，並對勝率 100% 但 MDD 極大等不直覺組合加上診斷說明。 |
| BACKTEST-ISSUE-014 | B-032 | 低 | 已修正待驗證 | 推薦回放重播頻率與資金配置功能可切換，但 hover 沒有說明。 | 已補上重播頻率、只跑一次、等權、分數加權等選項 tooltip，說明它們如何影響推薦回放與資金配置。 |
| BACKTEST-ISSUE-015 | B-033 / B-041 | 高 | 新增 | 推薦回放成功後結果頁資訊架構不佳：摘要重複顯示，期間明細、個股貢獻與交易紀錄被底部吃掉；資金使用與 Monte Carlo 數值也需要更好解釋。 | 重新設計推薦回放結果呈現：預設以概況卡顯示核心指標與 credibility warnings；期間明細、個股貢獻、交易紀錄預設折疊或放入分頁/右側 detail panel，點選後再展開；避免重複摘要，並補資金使用、Monte Carlo P05/P50/P95 的語意與單位。 |
| BACKTEST-ISSUE-016 | B-033 / B-041 | 中 | 已修正待驗證 | 推薦組合回測會在 terminal 顯示同日收盤訊號同日收盤成交的理想化假設 warning，但 UI 是否清楚揭露此限制仍需確認。 | 已將同日收盤成交假設、實盤時間差限制、gap / liquidity / cash / lot-size 風險加入 UI 摘要，不再只依賴 terminal `UserWarning`。 |
| BACKTEST-ISSUE-017 | B-035 | 高 | 已修正待驗證 | 推薦回放成功跑出結果後，「保存結果」仍無法點擊；使用者不知道保存按鈕啟用條件。 | 已確認推薦回放成功後保存按鈕會啟用，並補 tooltip 與保存成功訊息，顯示 Registry run ID、Profile、股票檔數與下一步。 |
| BACKTEST-ISSUE-018 | B-036 | 高 | 已修正待驗證 | 「升級為策略版本」點擊後沒有反應；使用者不知道是否因未保存、Gate 未通過或功能失效。 | 已補保存前 / 保存後 promote 按鈕 tooltip，說明需先保存、validation 不得 FAIL 或需載入歷史 run；更完整 gate result 展示仍可列為後續 UX 強化。 |
| BACKTEST-ISSUE-019 | B-037 | 中 | 新增 | 單股 Excel 報告可匯出，但 Traceability 顯示缺失欄位 `data_as_of_date`, `data_version`, `regime`, `benchmark`；使用者不確定這是否符合目前設計。 | 已修正待驗證：Excel「資料完整性」區塊已將缺失欄位顯示為中文名稱並保留原始欄位代號，例如「資料截止日期（data_as_of_date）」與「市場 Regime（regime）」。 |
| BACKTEST-ISSUE-020 | B-038 | 中 | 已修正待驗證 | 報酬分布與持有天數圖可顯示，但標題 / 軸 / legend 仍有英文，且使用者不知道如何判讀或是否正常。 | fast renderer 的圖表標題、軸、legend、Mean / Median marker 已中文化；更完整判讀說明與圖型設計仍可後續補強。 |
| BACKTEST-ISSUE-021 | B-038 / B-039 / B-042 | 中 | 需更多修改 | 歷史與比較、Registry 比較都需要手動點「重新整理」才讀取既有紀錄；圖表區也要先跑過任何一種測試後，才會出現以前的策略圖表。使用者點進 tab 時不知道是否有既有資料或舊圖表可看。 | 部分修正待驗證：保存、刪除、升級後已集中刷新歷史列表、圖表選單與 Registry 比較面板；首次進入自動載入與圖表舊資料載入仍屬後續 batch。 |
| BACKTEST-ISSUE-022 | B-040 / B-042 | 中 | 新增 | 批次結果與比較功能可操作，但使用者不清楚比較的意義，不知道應該比出什麼。 | 在批次結果與比較頁補「比較目的」與中文判讀：比較策略/參數/run 之間的報酬、風險、交易數、回撤、資料版本、成本、Universe 是否可比；不可比時用清楚中文 badge 解釋原因。 |
| BACKTEST-ISSUE-023 | B-041 / B-036 | 中 | 新增 | 推薦回放可成功保存並升級策略版本，但使用者不知道升級成策略後要去哪裡看、代表什麼。 | 部分修正待驗證：保存 / 刪除 / 升級成功後會顯示中文狀態提示並刷新相關 Registry 區塊；「前往策略版本」等深層導引按鈕仍屬後續 batch。 |
| BACKTEST-ISSUE-024 | B-042 | 高 | 新增 | Registry 比較頁不直觀且未中文化：run type、參數差異、Metrics、Regime、Benchmark 多為英文或內部代碼；表格內容被吃掉，Normalized Equity 空白不知如何使用；區塊間有白色框線不美觀。 | 部分修正待驗證：comparability reason 已中文化，例如「資料指紋不同」「成交假設不同」「Universe 股票池不同」；run type、表格排版、Normalized Equity 空狀態與整體 layout 重設仍屬後續 batch。 |

## 推薦分析

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| R-001 | 模式 | 新手 / 進階模式切換 | 切換文字、欄位顯示與設定保留合理 | 通過 | 新手 / 進階模式切換正常。 | 2026-06-22 人工驗證。 |
| R-002 | 市場狀態 | Regime 顯示 / 自動偵測 | 市場狀態可讀，錯誤時有提示 | 需確認 | 有顯示市場狀態，但只顯示「突破準備」與信心度；沒有 regime 代碼 / 分數 / 日期 / 資料來源。畫面是一段策略建議訊息，使用者不確定準確性，也不清楚模板組合有多少彈性或是否只有固定幾組。 | 2026-06-22 人工驗證，附截圖 `codex-clipboard-fc417f1d-875e-4768-b21d-0f26d05f8c63.png`；市場狀態透明度列 RECOMMEND-ISSUE-001。 |
| R-003 | 策略建議 | 一鍵套用建議 Profile | 套用後 Profile 與參數有變更且可辨識 | 需確認 | 可以切換不同新手策略並套用到進階設定；但使用者發現「暴衝策略」切到進階模式後策略傾向為「趨勢追蹤策略」，「穩健策略」對應「反轉」，「長期」對應「趨勢追蹤策略」，不確定是否符合設計。 | 2026-06-22 人工驗證；新手策略與進階策略傾向對照透明度列 RECOMMEND-ISSUE-002。 |
| R-004 | Profile | 選擇策略風格 | Profile 下拉可用，切換後設定同步 | 通過 | Profile 下拉可正常使用，目前只有三套預設且三個策略都有完整說明。使用者後續希望討論是否需要更多 Profile、是否支援自訂 Profile，以及何時/如何升級到此處。 | 2026-06-22 人工驗證；Profile 彈性與自訂流程列 RECOMMEND-ISSUE-003。 |
| R-005 | 策略傾向 | 偏好設定 | 設定變更會影響推薦配置 | 需確認 | 「策略傾向」區沒有可調的偏好設定，只是自動顯示目前選擇偏向哪些圖形模式或技術指標，沒有偏短線 / 偏長線等可選項。當偏向為「盤整 / 區間策略」時，文字與反白底色造成可讀性差；「反轉」與「趨勢」顯示較正常。 | 2026-06-22 人工驗證；偏好設定語意與可讀性列 RECOMMEND-ISSUE-004。 |
| R-006 | 技術指標 | 指標勾選 / 參數 | 啟用指標需完整參數；錯誤參數 fail-closed | 通過 | 技術指標勾選 / 參數功能正常。 | 2026-06-22 人工驗證。 |
| R-007 | 圖形模式 | 模式勾選 / 參數 | 圖形條件可配置，不產生未來函數假象 | 通過 | 圖形模式勾選 / 參數功能正常。 | 2026-06-22 人工驗證。 |
| R-008 | 篩選條件 | 最小漲幅 | 篩選條件進入推薦服務 | 通過 | 最小漲幅可調整且不能輸入負數；調大漲幅後推薦數量變少，確認條件有影響推薦結果。 | 2026-06-22 人工驗證。 |
| R-009 | 篩選條件 | 最小成交量比率 | 篩選條件進入推薦服務 | 已修正待驗證 | 最小成交量比率可調整，但只有數值，使用者不知道單位是百分比還是倍數；篩選條件三個欄位都沒有 hover 說明窗。 | 2026-06-23 已補最小漲幅百分比、成交量比率倍數單位與篩選欄位 tooltip；待使用者重測。 |
| R-010 | 篩選條件 | 產業篩選 | 產業清單可載入，全部 / 特定產業正常 | 通過 | 產業選擇功能正常。 | 2026-06-22 人工驗證。 |
| R-011 | 排名門檻 | 固定門檻 | fixed 模式結果與 metadata 可辨識 | 已修正待驗證 | 固定門檻可以選，但沒有其他可調整選項。 | 2026-06-23 已補固定門檻 tooltip 與推薦詳情中文化；是否要讓 fixed 門檻可調仍可後續討論。 |
| R-012 | 排名門檻 | 百分位排名 | quantile 模式需母體、百分位與 method metadata；母體過小有錯誤提示 | 已修正待驗證 | 最小母體數限制最低 20，但使用者不確定意思；排名方法目前只有 `nearest rack` / `nearest rank` 類似英文選項，需要中文化，也不確定是否應有其他選項。排名門檻內容都沒有 hover 說明窗口。 | 2026-06-23 已將排名方法中文化為「最近名次法」，並補百分位、母體數與 ranking detail 中文說明；待使用者重測。 |
| R-013 | 執行 | 執行推薦分析 | 背景執行、進度條、結果表與日誌正常 | 通過 | 執行推薦分析功能正常。 | 2026-06-22 人工驗證。 |
| R-014 | 結果 | 推薦理由詳情 | Why / Why Not / Explain 與分數拆解可讀 | 需確認 | 推薦理由詳情可正常顯示；但 ranking method、threshold mode 等欄位名稱與內容仍希望完整中文化。使用者也觀察到目前所有推薦都是 regime 不匹配，不知道要怎樣才會匹配 regime。 | 2026-06-23 已補 ranking method / threshold mode 中文化；regime 匹配說明仍列 RECOMMEND-ISSUE-008 待排查 / 設計。 |
| R-015 | 結果 | 保存結果 | 保存推薦配置、Profile、Regime 與名單 | 已修正待驗證 | 保存可以正常執行，但使用者不知道 Profile、regime、市場狀態與推薦名單是否都有被儲存，也不知道保存後去哪裡看。 | 2026-06-23 保存成功訊息已顯示 result ID、Profile、Regime、股票檔數與下一步；待使用者重測。 |
| R-016 | 結果 | 加入候選池 | 選取股票可加入 Watchlist | 已修正待驗證 | 加入候選池功能正常；使用者建議 UI 文案改成「加入觀察清單」更清楚且與其他頁一致。 | 2026-06-23 已統一可見文案為「加入觀察清單」；待使用者重測。 |
| R-017 | 結果 | 送 Research Lab 批次回測 | 切換到策略回測並帶入股票 / 配置 | 已修正待驗證 | 送 Research Lab 批次回測功能正常；但直接送批次時 list 名稱顯示為 `None_****`，使用者希望有更好的命名方式。 | 2026-06-23 已修正缺少來源名稱時不再顯示 `None`，改用 Profile / 推薦分析 fallback；待使用者重測。 |
| R-018 | 結果 | 送 Research Lab 推薦回放 | 切換到策略回測並帶入推薦回放配置 | 通過 | 送 Research Lab 推薦回放功能正常。 | 2026-06-22 人工驗證；與 B-033 流程一致。 |
| R-019 | 結果 | 匯出 Excel | 背景匯出，包含 metadata、配置、Regime 與推薦清單 | 通過 | 匯出 Excel 功能正常，且有記錄執行參數（Parameters）。 | 2026-06-22 人工驗證。 |
| R-020 | 結果右鍵 | 記錄到持倉 / 加入候選池 | 右鍵選單可用且保留推薦來源 metadata | 已修正待驗證 | 右鍵可選「記錄到持倉管理」與「加入候選池」，功能正常；使用者建議「加入候選池」改為「加入觀察清單」以維持一致。 | 2026-06-23 已統一右鍵文案為「加入觀察清單」；待使用者重測。 |

### 推薦分析 Issue 記錄簿

| Issue ID | 關聯項目 | 嚴重度 | 狀態 | 使用者觀察 | 初步處理方向 |
|---|---|---|---|---|---|
| RECOMMEND-ISSUE-001 | R-002 | 高 | 新增 | 推薦分析的市場狀態卡只顯示「突破準備」與信心度，缺少 regime 代碼、分數、日期、資料來源與模板彈性說明；使用者不確定準確性。 | 補上市場狀態來源、資料日期、分數 / confidence 的語意、推薦模板選擇依據與可用模板數量；信心度避免過度確定的 100% 文案，並與市場觀察的 regime 顯示一致。 |
| RECOMMEND-ISSUE-002 | R-003 | 中 | 新增 | 新手策略可切換，但「暴衝 / 穩健 / 長期」對應到進階模式的「趨勢追蹤 / 反轉」等策略傾向不透明，使用者不知道是否符合設計。 | 建立新手 Profile 到進階參數 / 策略傾向的對照說明；切換後顯示「已套用哪些條件、技術指標、圖形模式與原因」，讓使用者能確認是設計映射而非錯誤。 |
| RECOMMEND-ISSUE-003 | R-004 | 中 | 新增 | Profile 下拉目前只有三套預設，功能正常且說明完整，但使用者希望後續討論是否支援更多 Profile、自訂 Profile，以及 Research Lab 升級策略版本後是否可出現在此處。 | 釐清 Profile 生命週期：內建模板、自訂模板、從 Research Lab 升級而來的策略版本是否應進入推薦分析；若支援自訂，需設計保存、載入、版本與刪除流程。 |
| RECOMMEND-ISSUE-004 | R-005 | 中 | 新增 | 「策略傾向」不是可調偏好設定，而是自動摘要；缺少偏短線 / 偏長線等使用者可調偏好。偏向「盤整 / 區間策略」時文字與反白底色對比差，可讀性不佳。 | 重新命名或拆分區塊：若只是摘要，改名為「目前策略傾向摘要」；若要做偏好設定，新增明確可調控制。修正盤整 / 區間策略 chip 的文字顏色、底色與對比。 |
| RECOMMEND-ISSUE-005 | R-008 / R-009 / R-010 | 中 | 已修正待驗證 | 篩選條件功能大致正常，但最小成交量比率只有數值，使用者不知道單位是百分比或倍數；最小漲幅、最小成交量比率、產業篩選三個欄位都缺少 hover 說明。 | 已補篩選欄位單位與 tooltip：最小漲幅標示百分比；成交量比率標示倍數並說明流動性篩選；產業篩選說明「全部」與單一產業的效果。 |
| RECOMMEND-ISSUE-006 | R-011 / R-012 / R-014 | 中 | 已修正待驗證 | 排名門檻區可切換固定門檻 / 百分位排名，但固定門檻沒有可調選項；百分位排名的最小母體數、排名方法與 `nearest rank` 語意不清，且沒有 hover 說明。推薦詳情中 ranking method、threshold mode 等欄位名稱與內容也仍未完整中文化。 | 已補排名門檻區與推薦詳情中文 label、單位與 tooltip；`nearest rank` 顯示為「最近名次法」，`threshold mode` 顯示為「門檻模式」，內容顯示「固定門檻 / 百分位排名」。固定門檻是否要新增可調控制仍可後續討論。 |
| RECOMMEND-ISSUE-007 | R-015 | 中 | 已修正待驗證 | 推薦結果可保存，但保存後使用者不知道保存內容是否包含 Profile、regime、市場狀態與推薦名單，也不知道去哪裡查看。 | 保存成功訊息已顯示 result ID、保存內容摘要、Profile / Regime / 股票檔數與下一步入口；Manual 已補保存資料範圍。 |
| RECOMMEND-ISSUE-008 | R-014 | 中 | 新增 | 推薦詳情可顯示，但目前所有推薦都顯示 regime 不匹配，使用者不知道什麼條件下才會匹配 regime。 | 補上 regime match / mismatch 的判定規則、目前 market regime、profile 期望 regime 與不匹配影響；若全部不匹配是資料或 mapping 問題，需另行排查推薦策略與市場狀態對照。 |
| RECOMMEND-ISSUE-009 | R-016 / R-020 | 低 | 已修正待驗證 | 推薦結果與右鍵選單使用「加入候選池」文案，功能正常但與其他頁「加入觀察清單」不一致，使用者覺得後者更清楚。 | 已統一使用者可見文案為「加入觀察清單」，內部仍保留 watchlist / candidate pool 概念。 |
| RECOMMEND-ISSUE-010 | R-017 | 低 | 已修正待驗證 | 推薦結果送 Research Lab 批次回測功能正常，但批次 list 名稱顯示 `None_****`，不易理解來源。 | 已改善自動命名規則；缺少來源名稱時使用 Profile / 推薦分析 fallback，不再顯示 `None`。 |

## 觀察清單

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| W-001 | 候選池 | 刷新 | 候選池重新載入正常 | 通過 | 刷新候選池功能正常。 | 2026-06-22 人工驗證。 |
| W-002 | 候選池 | 新增股票 | 手動輸入股票成功，資料欄位合理 | 已修正待驗證 | 手動新增股票可成功，重複新增會被阻擋；但以 `2330` 測試時證券名稱沒有自動查找，代號與名稱都顯示 `2330`。新增不存在的股票代號也會成功加入。 | 2026-06-23 已補正式股票名稱查找與不存在代號阻擋；待使用者重測。 |
| W-003 | 候選池 | 移除選中 | 選取列可移除，有合理提示 | 通過 | 移除選中功能正常。 | 2026-06-22 人工驗證。 |
| W-004 | 候選池 | 清空候選池 | 有不可逆確認；清空後畫面更新 | 通過 | 清空候選池有提示，且確認後可正確清空。 | 2026-06-22 人工驗證。 |
| W-005 | 候選池 | 送 Research Lab 批次回測 | 目前若停用，需確認按鈕狀態與提示符合 Manual | 已修正待驗證 | 送 Research Lab 批次回測無法使用，點擊後沒有反應。 | 2026-06-23 已接 signal 並可直接切到 Research Lab 批次回測；空清單時 disabled 並顯示 tooltip 原因。 |
| W-006 | 候選池 | 保存為選股清單 | 可將候選池保存為 Universe | 通過 | 保存為選股清單功能正常。 | 2026-06-22 人工驗證。 |
| W-007 | 候選池右鍵 | 移除選取 | 右鍵移除與按鈕行為一致 | 通過 | 候選池右鍵移除功能正常。 | 2026-06-22 人工驗證。 |
| W-008 | 選股清單 | 刷新 | Universe 清單重新載入正常 | 通過 | 選股清單刷新功能正常。 | 2026-06-22 人工驗證。 |
| W-009 | 選股清單 | 載入到候選池 | 既有 Universe 可載入候選池 | 通過 | 選股清單載入到候選池功能正常。 | 2026-06-22 人工驗證。 |
| W-010 | 選股清單 | 新增 | 可建立 Universe 名稱、說明與股票內容 | 通過 | 新增選股清單功能正常。 | 2026-06-22 人工驗證。 |
| W-011 | 選股清單 | 編輯 | 可修改 Universe metadata / 股票清單 | 通過 | 編輯選股清單功能正常。 | 2026-06-22 人工驗證。 |
| W-012 | 選股清單 | 刪除 | 有確認；刪除後清單更新 | 通過 | 刪除選股清單功能正常。 | 2026-06-22 人工驗證。 |
| W-013 | 跨頁同步 | 從市場觀察 / 推薦加入後顯示 | 其他頁加入候選池後，切回觀察清單可看到 | 通過 | 跨頁同步功能正常。 | 2026-06-22 人工驗證。 |

### 觀察清單 Issue 記錄簿

| Issue ID | 關聯項目 | 嚴重度 | 狀態 | 使用者觀察 | 初步處理方向 |
|---|---|---|---|---|---|
| WATCHLIST-ISSUE-001 | W-002 | 高 | 已修正待驗證 | 手動新增股票可成功且會阻擋重複，但沒有自動查找證券名稱；輸入 `2330` 後代號與名稱都顯示 `2330`。不存在的股票代號也能加入候選池。 | 手動新增前已驗證股票代號是否存在於 stock master / SQLite / TWStockConfig 指定資料來源；存在時自動補證券名稱，不存在時阻擋並顯示清楚錯誤。 |
| WATCHLIST-ISSUE-002 | W-005 | 高 | 已修正待驗證 | 候選池「送 Research Lab 批次回測」點擊無反應，無錯誤提示也無 disabled 原因。 | 按鈕已接 signal；有股票時可切換 Research Lab 並帶入候選池股票清單，空清單時 disabled 並以 tooltip 顯示原因。 |

## 持倉管理

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| P-001 | 主操作 | 手動記錄交易 | 買 / 賣、價格、股數、日期、費用、稅金與來源欄位可保存 | 需確認 | 使用者希望自動帶入台股預設手續費與證交稅，需要時再手動修改；證券名稱沒有自動查詢，仍需使用者自行輸入。 | 2026-06-23 人工驗證；預設費率與股票名稱查找列 PORTFOLIO-ISSUE-001。 |
| P-002 | 主操作 | 新增日記 | 選取持倉後可新增覆盤日誌 | 通過 | 新增日記功能正常。 | 2026-06-23 人工驗證。 |
| P-003 | 主操作 | 整理刷新 | 持倉、交易、日誌與監控重新整理 | 通過 | 整理刷新功能正常。 | 2026-06-23 人工驗證。 |
| P-004 | 主操作 | 清空全體數據 | 二次確認；預設只測取消流程，避免破壞資料 | 通過 | 清空全體數據有提示，流程正常。 | 2026-06-23 人工驗證；高風險功能已由使用者確認提示存在。 |
| P-005 | 活躍持倉 | 持倉列表 | 平均成本、投入資金、目前狀態與來源摘要正確 | 需確認 | 使用者不確定 P-005 是否指上方卡片；上方卡片顯示活躍持倉部位 2 檔，但沒有顯示是哪幾檔。四大卡片字體太小，看起來奇怪，UI 也可更好看。 | 2026-06-23 人工驗證；活躍持倉摘要與 UI 改善列 PORTFOLIO-ISSUE-002。 |
| P-006 | 當前持倉部位 (Position) 右鍵 | 為此部位寫日記 | 右鍵建立日記並帶入股票 | 通過 | 為部位寫日記功能正常。 | 2026-06-23 人工驗證；後續測試說明需以 UI 名稱「當前持倉部位 (Position)」描述，避免「活躍持倉」造成混淆。 |
| P-007 | 當前持倉部位 (Position) 右鍵 | 只查看此股交易歷史 | 交易歷史可依股票過濾 | 通過 | 只查看此股交易歷史功能正常。 | 2026-06-23 人工驗證。 |
| P-008 | 當前持倉部位 (Position) 右鍵 | 顯示全部交易歷史 | 清除過濾正常 | 未通過 | 右鍵或相關操作中沒有「顯示全部交易歷史」。 | 2026-06-23 人工驗證；清除交易歷史過濾入口列 PORTFOLIO-ISSUE-003。 |
| P-009 | 交易歷史 | 檢視交易明細 | 選取持倉後交易歷史同步 | 通過 | 交易歷史檢視交易明細功能正常。 | 2026-06-23 人工驗證。 |
| P-010 | 交易歷史右鍵 | 刪除此交易紀錄 | 有確認；刪除後持倉與平均成本重算 | 通過 | 交易歷史右鍵刪除此交易紀錄功能正常。 | 2026-06-23 人工驗證。 |
| P-011 | 覆盤日誌 | 檢視日誌 | 選取持倉後日誌同步 | 通過 | 覆盤日誌檢視功能正常。 | 2026-06-23 人工驗證。 |
| P-012 | 覆盤日誌右鍵 | 刪除此篇日記筆記 | 有確認；刪除後列表更新 | 通過 | 覆盤日誌右鍵刪除此篇日記筆記功能正常。 | 2026-06-23 人工驗證。 |
| P-013 | 策略與價格監控 | 目前價格 / 未實現損益 | 價格、PnL、停損停利與狀態原因顯示 | 需確認 | 策略與價格監控功能正常；但最新價格只顯示價格，使用者希望同時顯示該價格是哪一天。 | 2026-06-23 人工驗證；最新價格日期列 PORTFOLIO-ISSUE-004。 |
| P-014 | 策略與價格監控 | 來源追溯 | 推薦 / 回測 / 策略版本來源 metadata 可讀 | 需確認 | 手動建立的持倉顯示策略與回測版本為手動建立，推薦及版本細節為未知；使用者推測應屬合理，但希望語意清楚。 | 2026-06-23 人工驗證；手動來源未知語意列 PORTFOLIO-ISSUE-005。 |
| P-015 | 籌碼監控 | 籌碼風險與分點明細 | chip risk、近期分點買賣與品質狀態顯示 | 需確認 | 有顯示籌碼風險、連續買賣超紀錄與追蹤分點近五日買賣明細；但風險評級顯示英文，使用者希望改為繁體中文。 | 2026-06-23 人工驗證；籌碼風險中文化列 PORTFOLIO-ISSUE-006。 |
| P-016 | 籌碼監控 | 下鑽詳細主力流向 | 可切到市場觀察 > 主力流向並定位該股票 | 需確認 | 可跳轉到市場觀察 > 主力流向，但沒有定位到目前這檔股票。 | 2026-06-23 人工驗證；主力流向下鑽定位列 PORTFOLIO-ISSUE-007。 |
| P-017 | 跨頁來源 | 從推薦 / 回測記錄到持倉 | 來源 metadata 保留，且不自動下單 | 未通過 | 從推薦分析記錄到持倉時跳錯誤：`無法記入交易，領域規則校驗失敗：Object of type Decimal is not JSON serializable`。Research Lab 批次結果頁目前沒有可加到持倉的選項，使用者不確定「結果頁」指的是哪裡。 | 2026-06-23 人工驗證；Decimal JSON 序列化錯誤與 Research Lab 入口不清列 PORTFOLIO-ISSUE-008 / PORTFOLIO-ISSUE-009。 |

### 持倉管理 Issue 記錄簿

| Issue ID | 關聯項目 | 嚴重度 | 狀態 | 使用者觀察 | 初步處理方向 |
|---|---|---|---|---|---|
| PORTFOLIO-ISSUE-001 | P-001 | 中 | 已修正待驗證 | 手動記錄交易時希望自動帶入台股預設手續費與證交稅，需要時再由使用者手動修改；證券名稱沒有自動查詢，仍需自行輸入。 | 手動交易表單已依台股預設手續費率與證交稅率自動估算，仍可手動覆寫；輸入股票代號後會查詢 stock master / SQLite 自動補證券名稱，不存在代號會提示。 |
| PORTFOLIO-ISSUE-002 | P-005 | 中 | 已修正待驗證 | 活躍持倉摘要卡顯示有 2 檔，但沒有顯示是哪幾檔；上方四大卡片字體太小，視覺設計不佳。 | 活躍持倉區已新增摘要列，顯示持倉檔數與前幾大持倉代號 / 名稱；四大卡片整體視覺優化留待後續 batch。 |
| PORTFOLIO-ISSUE-003 | P-008 | 中 | 已修正待驗證 | 在「當前持倉部位 (Position)」依股票過濾交易歷史後，沒有「顯示全部交易歷史」入口可清除過濾。 | 交易歷史區已新增過濾狀態提示與「清除篩選」入口，可回到全部交易歷史。 |
| PORTFOLIO-ISSUE-004 | P-013 | 中 | 已修正待驗證 | 策略與價格監控顯示最新價格，但沒有顯示價格日期，使用者不知道該價格是哪一天。 | 最新價格旁已顯示價格日期 / as-of date；資料品質 warning 的更完整呈現留待後續 batch。 |
| PORTFOLIO-ISSUE-005 | P-014 | 低 | 已修正待驗證 | 手動建立持倉時，推薦及版本細節顯示未知大致合理，但使用者需要明確知道這是因為來源為手動建立，而非資料缺失。 | 手動持倉來源已顯示為「手動建立，無推薦 / 回測來源」，避免把手動建立誤解為資料缺失。 |
| PORTFOLIO-ISSUE-006 | P-015 | 低 | 已修正待驗證 | 籌碼監控功能可顯示籌碼風險、連續買賣超與追蹤分點近五日明細，但風險評級仍顯示英文。 | 籌碼風險等級與品質狀態已中文化，並在 tooltip 保留 raw key 供除錯。 |
| PORTFOLIO-ISSUE-007 | P-016 | 中 | 已修正待驗證 | 籌碼監控可跳轉到市場觀察 > 主力流向，但沒有定位到目前持倉股票。 | 籌碼監控下鑽時已將目前股票代號傳給主視窗，供市場觀察主力流向定位 / 篩選。 |
| PORTFOLIO-ISSUE-008 | P-017 | 高 | 已修正待驗證 | 推薦分析右鍵「記錄到持倉管理」失敗，錯誤為 `Object of type Decimal is not JSON serializable`。 | Portfolio JSONL 寫入已新增遞迴 Decimal sanitizer，Decimal 以字串保存，避免破壞金額 / 股數精度。 |
| PORTFOLIO-ISSUE-009 | P-017 | 中 | 已修正待驗證 | Healthcheck 中「Research Lab 結果頁」入口語意不清；使用者在批次結果頁找不到加入持倉選項。 | Manual 已補充目前可記錄到持倉的入口以單股回測交易與推薦 / 回測來源交易為主；批次結果頁不應被描述為已提供直接加入持倉入口。 |

## Runtime Observatory

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| RT-001 | 初始化 | 分頁載入 | Runtime 初始化成功；失敗不影響其他分頁 | 通過 | Runtime Observatory 分頁可正常載入。 | 2026-06-23 人工驗證，附截圖 `codex-clipboard-87ca7faa-beab-4e5e-b7de-2ef62ad88d43.png`。 |
| RT-002 | Objective | 目前任務目標 | Objective 欄位可顯示或明確空狀態 | 需確認 | Objective 顯示在 FSM State Machine 區塊，目前為 `No task assigned`；使用者希望改成繁體中文，並需要更多說明告訴他這個頁面 / Objective 什麼時候會被觸發。 | 2026-06-23 人工驗證；中文化與操作說明列 RUNTIME-ISSUE-001。 |
| RT-003 | Task Workflow Status | FSM 狀態 | 狀態更新隨 timer poll 變化 | 需確認 | FSM State Machine 包含 Objective 與 Task Workflow Status；使用者不理解 FSM 是什麼，目前狀態顯示 `IDLE`，需要中文化與說明。 | 2026-06-23 人工驗證；FSM 中文化與說明列 RUNTIME-ISSUE-001。 |
| RT-004 | Overall System State / Health | 系統健康 | 健康資訊、拒絕率、錯誤率可讀 | 需確認 | Governance Health 顯示 Overall System State = `HALTED`、Rejection Rate = `80.0% (↑)`、Consecutive Fails = `4`，使用者不知道代表什麼、何時觸發，也需要中文化。 | 2026-06-23 人工驗證；Governance Health 判讀列 RUNTIME-ISSUE-002。 |
| RT-005 | Last Critical Violation | 最近重大違規 | 無事件時有空狀態；有事件時可讀 | 需確認 | Last Critical Violation 顯示 `GovernanceViolation`，可顯示事件但仍是英文工程代碼，使用者看不懂。 | 2026-06-23 人工驗證；重大違規中文化列 RUNTIME-ISSUE-002。 |
| RT-006 | Append-only Event Stream | 事件流 | 時間、嚴重度、actor、event type、訊息正常追加 | 需確認 | Append-only Event Stream 可顯示事件流，包含時間、INFO、actor 與 event type；但 `test_start`、`agent_output_received`、`validation_rejected`、`JSONDecodeError`、`SchemaViolation`、`GovernanceViolation` 等仍為英文 / 工程代碼，使用者需要中文摘要與判讀說明。 | 2026-06-23 人工驗證；事件流中文化與可讀性列 RUNTIME-ISSUE-003。 |

### Runtime Observatory Issue 記錄簿

| Issue ID | 關聯項目 | 嚴重度 | 狀態 | 使用者觀察 | 初步處理方向 |
|---|---|---|---|---|---|
| RUNTIME-ISSUE-001 | RT-002 / RT-003 | 中 | 已修正待驗證 | FSM State Machine 中 Objective 顯示 `No task assigned`、Task Workflow Status 顯示 `IDLE`，且使用者不理解 FSM / Objective 何時被觸發。 | Runtime Observatory 已將目標與流程狀態中文化，並補充其只反映 Runtime / Governance 任務，平常閒置屬正常。 |
| RUNTIME-ISSUE-002 | RT-004 / RT-005 | 高 | 已修正待驗證 | Governance Health 顯示 `HALTED`、`Rejection Rate 80.0%`、`Consecutive Fails: 4`、`GovernanceViolation`，使用者不知道意義或觸發條件。 | Governance Health 已中文化 HALTED、驗證拒絕率、連續失敗次數與重大違規，並補充治理暫停不等同主 app 全域故障。 |
| RUNTIME-ISSUE-003 | RT-006 | 中 | 已修正待驗證 | Append-only Event Stream 可顯示事件，但 event type 與錯誤原因多為英文工程代碼，使用者難以判讀。 | 事件流已顯示繁中摘要與嚴重度，raw event type / payload 保留在 tooltip 供除錯。 |
| RUNTIME-ISSUE-004 | RT-002 / RT-003 / RT-004 | 中 | 已修正待驗證 | 使用者在資料更新的券商分點下載執行中查看 Runtime Observatory，但頁面仍顯示 `IDLE` / `No task assigned`；使用者不確定 Runtime Observatory 是否應監控資料更新任務。 | UI 已明確標示 Runtime Observatory 不監控資料更新背景任務；資料更新、回測與推薦長任務仍由各自頁面顯示狀態。 |

## 全域與跨工作區流程

| ID | 流程 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|
| X-001 | 啟動主程式 | `ui_qt/main.py` 可啟動；主視窗、樣式、icon、status bar 正常 | 通過 | 整輪人工 healthcheck 可正常操作主視窗與各 tab。 | 2026-06-23 由完整人工檢查流程間接驗證。 |
| X-002 | Tab 順序 | 數據更新、市場觀察、每日決策、策略回測、推薦分析、觀察清單、持倉管理、Runtime Observatory 順序符合目前程式 | 通過 | 使用者已依序完成數據更新、市場觀察、每日決策、策略回測、推薦分析、觀察清單、持倉管理與 Runtime Observatory 檢查。 | 2026-06-23 由完整人工檢查流程驗證。 |
| X-003 | 市場觀察自動載入 | 切換到市場觀察子頁時，強弱股 / 產業 / 主力流向會需要時載入 | 需確認 | 市場觀察子頁功能面多數通過，但強 / 弱勢個股與產業載入有明顯卡頓，主力流向圖表 / 語意仍需改善。 | 詳見 MARKET-ISSUE-003 / MARKET-ISSUE-006 / MARKET-ISSUE-007。 |
| X-004 | 推薦到回測 | 推薦分析可把批次 / 推薦回放配置送到 Research Lab | 通過 | 推薦分析送 Research Lab 批次回測與推薦回放皆可用。 | 詳見 R-017 / R-018；批次 list 命名已另列 RECOMMEND-ISSUE-010 修正待驗證。 |
| X-005 | 推薦 / 回測到持倉 | 右鍵記錄交易可保留來源追溯 | 未通過 | 推薦分析記錄到持倉時跳錯：`Object of type Decimal is not JSON serializable`；Research Lab 批次結果頁也沒有明確加入持倉入口。 | 詳見 P-017、PORTFOLIO-ISSUE-008 / PORTFOLIO-ISSUE-009。 |
| X-006 | 持倉到主力流向 | 籌碼監控下鑽可切頁並定位股票 | 需確認 | 持倉籌碼監控可跳到市場觀察 > 主力流向，但沒有定位目前股票。 | 詳見 P-016、PORTFOLIO-ISSUE-007。 |
| X-007 | Session context strip | 狀態列研究 session context 顯示不遮擋、不報錯 | 通過 | 多張截圖顯示底部 Session / Symbol / Regime / Profile / Watchlist 狀態列可顯示且未回報遮擋或錯誤。 | 2026-06-23 由人工檢查截圖與使用流程間接驗證。 |

## 待後續整理的失敗紀錄

### Issue 狀態規則

後續修復時，本檔採用 issue 記錄簿模式：

| 狀態 | 意義 |
|---|---|
| 已記錄 | 已由人工驗證發現並寫入本檔，尚未修正。 |
| 修正中 | 已開始分析或修改，但尚未完成。 |
| 已修正待驗證 | 程式已修正，但尚未由使用者重新操作驗證。 |
| 驗證通過 | 使用者已重新驗證，確認問題消失。 |
| 重開 | 修正後仍重現，或衍生出同一根因問題。 |

原則：修正完成後只能先標示為「已修正待驗證」，不得直接宣稱完成；必須等使用者再次驗證後，才能改成「驗證通過」。

### 數據更新 Tab Issue 記錄簿

| Issue ID | 來源功能 ID | 問題分類 | 問題摘要 | 目前狀態 | 修正狀態 | 驗證狀態 | 優先級 | 後續處理 |
|---|---|---|---|---|---|---|---|---|
| UPDATE-ISSUE-001 | U-002 / U-003 | 背景任務 / 分頁切換 | 全部資料更新中切換 subtab 後不再繼續。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | UpdateView 改為追蹤多個 active workers；狀態查詢 / 切頁不再取消既有更新 worker。 |
| UPDATE-ISSUE-002 | U-008 | 背景任務 / UI 狀態 | 每日股價合併 CSV 中切 tab 後卡住，後續匯出 CSV 完成也不會解除「合併中」。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 合併與匯出各自保留 worker reference；完成 / error signal 後釋放自身 worker，避免互相覆蓋。 |
| UPDATE-ISSUE-003 | U-022 | 背景任務 / 分頁切換 | 券商分點合併中切 tab 後卡住。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 券商分點合併 worker 不再被切頁觸發的狀態查詢覆蓋。 |
| UPDATE-ISSUE-004 | U-023 | 背景任務 / 分頁切換 | 技術指標計算中切 tab 後中斷，不會背景繼續。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 技術指標計算 worker 改由 active worker registry 保留到任務完成。 |
| UPDATE-ISSUE-005 | U-024 | SQLite 檢視 UX | SQLite 檢視單一日期與日期區間只能手打，希望改為可點選日期 / 日曆。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：SQLite 三個日期欄位預設空白，按「日曆」會跳到今天月份。 |
| UPDATE-ISSUE-006 | U-024 | 語言 / 欄位顯示 | SQLite 檢視 `daily_price` / `daily_prices` 顯示簡體中文欄位「漲跌」。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 新建 daily_prices schema 改為繁中 `漲跌`；舊 DB 若仍有 `涨跌`，Inspector 顯示 alias 為 `漲跌`。 |
| UPDATE-ISSUE-007 | U-024 / U-023 | 資料語意 / 技術指標可信度 | 漲跌欄位空白，漲跌價差全為正且整欄綠色，方向似乎另在 `漲跌(+/-)`；連帶懷疑 technical indicators 是否用錯誤數據計算。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | Inspector 的 `漲跌價差` 依 `漲跌(+/-)` / `漲跌` 方向顯示正負；技術指標計算路徑使用 OHLC 價格欄位，不直接以 `漲跌價差` 作為指標計算基礎。 |
| UPDATE-ISSUE-008 | U-024 | SQLite 檢視 UX / 效能 | SQLite 檢視所有欄位不能排序。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 表頭點擊會以白名單欄位送出 server-side `ORDER BY`，沿用既有分頁查詢避免 UI 端全量排序。 |
| UPDATE-ISSUE-009 | U-002 / U-003 | TPEX 更新韌性 | TPEX official endpoint timeout 會讓快速更新顯示失敗。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | TPEX 失敗改為 workflow warning，其他資料源繼續同步；日誌與完成視窗顯示 TPEX 診斷。 |
| UPDATE-ISSUE-010 | U-024 | SQLite 檢視 UX | SQLite 檢視日期預設為 1900，且欄位寬度不足，需要手動拉大 UI。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：SQLite 三個日期欄位不再顯示 `1900-01-01`，預設為空白；日曆開啟時定位今天月份。 |
| UPDATE-ISSUE-011 | U-024 | SQLite 表格顯示 | 查詢 `3207` 時，PandasTableModel 因重複欄名 alias 取到 Series，出現 truth value ambiguous 錯誤。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 表格 display data 改以欄位位置取值，避免重複顯示名稱造成 Series ambiguity。 |
| UPDATE-ISSUE-012 | U-022 | SQLite 同步 / 唯一鍵 | 券商分點同一分點 / 股票 / 日期同時存在 `買超` 與 `賣超` 時，舊唯一鍵造成衝突。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | broker sync 前受控備份並升級 `broker_flows` 主鍵納入 `trade_type`；合併去重鍵也納入 `trade_type`。 |
| UPDATE-ISSUE-013 | U-021 / U-022 | 效能 / 券商分點更新 | 券商分點資訊一次更新約 40 個分點時耗時明顯過長，需評估是否可支援受控並行，例如 5、10 或更高並行數，並確認網頁端是否會阻擋。 | 已記錄 | 未修正 | 待排查 | 中 | 先排查目前券商分點下載 / 合併是否為序列流程、是否已有 rate limit / retry / session 限制；後續若可行，需採受控 concurrency 與保守預設。 |
| UPDATE-ISSUE-014 | U-023 | 效能 / 技術指標計算 | 技術指標目前疑似 inline / 單核心計算；需評估是否可用 4 core 等多核心並行計算以縮短全市場計算時間。 | 已記錄 | 未修正 | 待排查 | 中 | 先排查目前計算流程是否逐股序列、是否受 SQLite 寫入或 CSV I/O 限制；若要並行，需拆分計算與寫入階段，避免多進程同時寫 DB 或覆寫 CSV。 |
| UPDATE-ISSUE-015 | U-024 / U-007 / U-008 | TPEX / SQLite 同步缺口 | SQLite 檢視 `3207` 只有近兩天資料，顯示 TPEX 日常資料已開始寫入但尚未補齊 2014 起歷史資料；需確認歷史 TPEX 回補是否未執行。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 已改用 TPEX historical afterTrading endpoint 回補並同步 SQLite；2026-06-17 排查確認 `daily_prices` 中 `3207` 已有 `20140102` 至 `20260617`、共 2907 筆。 |
| UPDATE-ISSUE-016 | U-007 / U-008 / U-024 | TPEX / 每日股價手動同步 | 點每日股價的手動下載資料源或後續同步後，SQLite 檢視 `daily_prices` 查不到 `3207`，但 TPEX CSV 未消失；疑似手動每日股價路徑未納入 `daily_price_tpex`，或同步時覆蓋了同日 TPEX rows。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 每日股價手動下載改為 TWSE + TPEX 區間流程，完成後同步 `daily_price_files` 到 SQLite 並執行技術指標增量；測試替身保留舊單日 API fallback。 |
| UPDATE-ISSUE-017 | U-002 / U-003 | 使用者理解 / 更新模式語意 | 快速更新與完整更新差異不清楚：快速更新後 metadata 的市場 / 產業 CSV、`daily_price` 與 `broker_flow` daily 檔也會更新；完整更新又重跑一次全部，使用者不確定完整更新額外做了哪些檢查或資料重建。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | UI tooltip 與 Manual 已更新：快速更新只跑結束日前最近 2 天並跳過大型合併；安全更新依 UI 日期範圍補齊並重建完整 CSV + SQLite。 |
| UPDATE-ISSUE-018 | U-023 / U-024 | 技術指標 / SQLite 欄位相容性 | TPEX 歷史 daily 寫入 SQLite 後，全量技術指標背景計算失敗，訊息為「沒有有效的數據可以合併」；`daily_prices` 已有 `3207` 歷史資料，但 `technical_indicators` 原本沒有 `3207`。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 根因為技術指標合併與 SQLite 寫入仍檢查舊 mojibake 欄名，且 `daily_prices` 同時存在正確欄名與舊 alias 欄位，rename 後造成 duplicate `日期`；已修正欄位 canonicalization，正式狀態檢查顯示 `technical_indicators` 最新日期 `2026-06-17`、總筆數 5,126,249。 |
| UPDATE-ISSUE-019 | U-001 / U-024 | 資料狀態 / SQLite 欄位相容性 | 開啟 App 檢查資料狀態時，`market_indices`、`industry_indices`、`broker_flows`、`technical_indicators` 狀態查詢仍使用舊 mojibake 欄名，也就是 `日期`、`證券代號` 等欄位被錯誤編碼後的版本，導致 no such column error。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | `UpdateService` 的 SQLite status helper 改為依實際 table columns 選擇 canonical 欄位，優先使用 `日期`、`證券代號`、`分點名稱`，必要時才 fallback 舊 alias；避免資料狀態頁因 schema canonicalization 後查錯欄位。 |
| UPDATE-ISSUE-020 | U-001 / U-024 | 文字編碼 / DB schema 清理 | App log、狀態檔與 `daily_prices` schema 殘留 mojibake 文字，造成使用者看到不可讀訊息，且可能讓後續程式誤用舊欄位。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 已修復 `app_module/update_service.py` 內正式路徑可見亂碼字串與註解，修復 `meta_data` 狀態 JSON 舊訊息；SQLite `daily_prices` 的空白亂碼欄位已在備份 `D:/Min/Python/Project/FA_Data/sqlite/twstock_before_drop_mojibake_col_20260617_235005.db` 後移除，最終 schema 掃描未再發現 mojibake 欄位。 |
| UPDATE-ISSUE-021 | U-021 / U-022 | 效能 / 券商分點跳過判斷 | 快速更新券商分點約 5 分鐘，明明近兩天才更新過，不應重新抓取所有既有資料。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 根因之一是 SQLite 既有資料檢查用 `分點名稱 = branch_system_key`，但 DB 實際常存顯示名稱；已改為同時比對分點顯示名稱與 system key，已有資料時不啟動 MoneyDJ 頁面抓取。受控並行仍列在 UPDATE-ISSUE-013 待排查。 |
| UPDATE-ISSUE-022 | U-023 | 技術指標 / CSV 增量合併 | 技術指標計算日誌出現大量「缺少日期欄位，將直接合併數據」，多個單股指標檔筆數倍增。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | `calculate_and_store_indicators()` 會先將新結果 `Date` 正規化為 `日期`；若舊檔或新結果仍缺少可辨識日期，改為覆蓋新結果而非直接 concat。`UpdateService.calculate_technical_indicators()` 也修正同名欄位 canonicalization 誤刪 `日期` / `證券代號`。 |
| UPDATE-ISSUE-023 | U-001 / U-025 | 全部資料看板 / 月營收狀態 | 「全部資料」看板目前沒有月營收狀態卡，使用者看不到月營收最新日期、筆數與狀態。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 2026-06-23 已在全部資料狀態卡區加入「月營收資料」，顯示 `fundamental_monthly_revenues` 最新年月、筆數與狀態；待使用者重測。 |
| UPDATE-ISSUE-024 | U-005 | 每日股價日期 UX | 手動下載日期範圍在多個子 tab 同步，但結束日期的日曆圖示不清楚，缺少一鍵今日，日曆開啟時需預設今天且日期格不能顯示成 `...`。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：每日股價 / 大盤 / 產業 / 券商分點的「結束日期」旁已是清楚的「日曆」按鈕。 |
| UPDATE-ISSUE-025 | U-028 / U-029 | 背景補齊 / 技術指標跳過判斷 | 背景 TPEX 補齊狀態顯示 `技術指標: running`，但每日股價與技術指標已同日，應先檢查並跳過，不應強制全量或 250 天回看。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | UI 背景任務移除 `--technical-force-all`；腳本先比對 latest date，已最新時寫入 skipped，落後時才用 120 天 warmup。 |
| UPDATE-ISSUE-026 | U-024 | SQLite 檢視 / 券商分點篩選 | `broker_flows` 的券商分點名稱難記，純文字輸入不利驗證。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：選 `broker_flows` 後，券商分點旁按「展開」可看到下拉清單。 |
| UPDATE-ISSUE-027 | U-024 | SQLite 檢視 / 日期篩選 UX | 日期預設不應先填；日曆打開需跳今天；清除按鈕過多；單一日期要有今日按鈕；區間旁一個清除要同時清掉單一與區間；日曆圖示與日期格顯示不清。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：SQLite 三個日期欄位預設空白，日期按「日曆」會跳到今天月份。 |
| UPDATE-ISSUE-028 | U-008 | 每日股價合併訊息 / TPEX 大表合併 | `daily_price_tpex/` 已有 0618 日檔，但合併 `stock_data_whole.csv` 完成提示仍停在 0617；根因是合併腳本只掃 `daily_price/`，未納入 TPEX 日檔目錄。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | `scripts/merge_daily_data.py` 改為同時掃 `daily_price/` 與 `daily_price_tpex/`；新增回歸測試覆蓋「既有大表 0617、TPEX 0618」時合併後最新日期應為 0618。 |
| UPDATE-ISSUE-029 | U-001 / U-024 | 資料狀態 / SQLite 同步缺口 | 快速更新後檢查數據狀態顯示最新日期為 0622，但 SQLite 檢視 `daily_price` / `daily_prices` 查 `2330` 只有到 0617；手動下載最近十天後新增 0622，但仍缺 0618；`3207` 則正常。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 根因：TWSE `daily_price/20260618.csv` 不存在，而 TPEX `daily_price_tpex/20260618.csv` 存在；`UpdateService.update_daily()` 與 `scripts/batch_update_daily_data.py` 都從 `start_date + 1` 開始檢查 / 下載，導致第一個缺漏日 0618 被跳過。已改為包含開始日期，並新增回歸測試。06/19 為 TWSE 端午節休市，缺資料屬正常。 |
| UPDATE-ISSUE-030 | U-006 / U-020 | 狀態檢查 UX | 每日股價與券商分點點「檢查此資料源狀態」後只看到完成訊息，使用者不知道狀態結果在哪裡、該看哪個日期 / 筆數 / 燈號。 | 已記錄 | 已修正待驗證 | 待驗證 | 中 | 每日股價與券商分點資料源頁面已新增檢查摘要列，顯示最新日期、筆數、SQLite / CSV 狀態與缺漏提示；日誌保留細節。 |
| UPDATE-ISSUE-031 | U-010 | 高風險操作 UX | 每日股價「強制重新合併所有每日股價」沒有清楚取消按鈕或取消流程；使用者不理解如何只測取消流程。 | 已記錄 | 已修正待驗證 | 待驗證 | 高 | 高風險操作已改為明確二次確認對話框，包含「取消」與「確認強制合併」，並說明會重建衍生資料、不應亦不會修改或刪除 raw CSV 原始檔。 |

### 數據更新 Tab 收斂狀態

2026-06-18 使用者回報數據更新 Tab 功能面已完成人工驗證；本次補記的人工驗證項目包含每日股價 / 大盤 / 產業 / 券商分點結束日期日曆按鈕、SQLite 日期欄位預設空白、SQLite 日曆定位今天月份、`broker_flows` 券商分點下拉清單。

目前剩餘項目歸類為非功能或後續工程，不阻擋本輪功能驗收：

| Issue ID | 類型 | 後續事項 | 目前狀態 |
|---|---|---|---|
| UPDATE-ISSUE-013 | 效能評估 | 券商分點下載 / 合併是否可做受控並行，並確認 rate limit、retry 與站方阻擋風險。 | 已記錄，未修正，待排查 |
| UPDATE-ISSUE-014 | 效能評估 | 技術指標計算是否可拆成多核心計算與單點寫入，避免 SQLite / CSV 寫入競爭。 | 已記錄，未修正，待排查 |

### 原始失敗紀錄

| ID | 來源功能 ID | 問題摘要 | 重現步驟 | 實際結果 | 預期結果 | 嚴重度 | 是否需要修復任務 |
|---|---|---|---|---|---|---|---|
| ISSUE-001 | U-002 / U-003 | 全部資料更新切換 subtab 後不再繼續 | 在「全部資料」點更新，尚未完成時切到其他 subtab。 | 更新不會繼續跑。 | 長任務應在背景繼續，UI 可切頁且完成後正確復原狀態。 | 高 | 是 |
| ISSUE-002 | U-008 | 每日股價合併中切 tab 後卡住 | 在「每日股價」執行合併 CSV，尚未完成時切到其他 tab；再執行匯出 CSV。 | 匯出 CSV 可完成，但合併仍顯示「合併中」，像是任務或 UI 狀態卡住。 | 合併任務應背景完成或明確失敗；按鈕與進度狀態必須復原。 | 高 | 是 |
| ISSUE-003 | U-022 | 券商分點合併中切 tab 後卡住 | 在「券商分點」執行合併，未結束時切到其他 tab。 | 會卡住，不能正常完成 / 復原。 | 合併任務應背景繼續，切頁不應中斷或卡住。 | 高 | 是 |
| ISSUE-004 | U-023 | 技術指標計算中切 tab 後中斷 | 在「技術指標」執行計算，未結束時切到其他 tab。 | 任務中斷，不會背景繼續。 | 技術指標計算應在背景繼續或提供明確取消 / 失敗狀態。 | 高 | 是 |
| ISSUE-005 | U-024 | SQLite 檢視日期篩選只能手打 | 在 SQL 資料檢視使用單一日期或區間篩選。 | 日期只能手動輸入。 | 希望提供日期選擇器 / 日曆 picker，減少輸入錯誤。 | 中 | 是 |
| ISSUE-006 | U-024 | SQLite 檢視出現簡體中文欄位 | 檢視 `daily_price` / `daily_prices` table。 | 「漲跌」欄位顯示簡體中文。 | 全系統使用者可見文字必須是繁體中文。 | 高 | 是 |
| ISSUE-007 | U-024 / U-023 | 漲跌欄位語意與顏色疑似錯誤，可能影響技術指標可信度 | 檢視 `daily_price` / `daily_prices` 與 `technical_indicators`。 | 「漲跌」欄位空白；「漲跌價差」都是正值且整欄綠色；方向似乎另在 `漲跌(+/-)` column；因此懷疑 technical indicators 是否以錯誤數據計算。 | 漲跌方向、價差正負號、顏色與技術指標輸入欄位必須語意一致且可追溯。 | 高 | 是 |
| ISSUE-008 | U-024 | SQLite 檢視所有欄位不能排序 | 在 SQLite 資料檢視點表格欄位。 | 所有欄位都不能排序。 | 希望能快速依指定欄位排序，同時不要大幅拖慢大表格。 | 中 | 是 |

## 本次未測 / 跳過原因

| 功能 ID | 原因 | 後續建議 |
|---|---|---|
|  |  |  |

## 備註

- 高風險資料操作（強制全量、清空、刪除正式紀錄）優先只驗證「取消 / 防呆 / 提示」流程；若要實際執行，需另行確認並先備份。
- 策略、回測、推薦、資金、倉位、風控與績效相關失敗，後續修復前需先做 Look-ahead bias 自查與金融數值邊界檢查。
- 本檔新增後不代表 Application Manual 功能行為有改變，因此不更新 Snapshot / Roadmap / Manual。
