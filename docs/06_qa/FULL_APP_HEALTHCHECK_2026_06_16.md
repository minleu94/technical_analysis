# 全程式功能健檢紀錄

> 建立日期：2026-06-16
> 範圍：PySide6 主 UI（`ui_qt/main.py`）目前 8 個頂層工作區與其子分頁 / 主要操作。
> 原則：本檔只記錄人工驗證結果與觀察，不直接提出或執行修復。

## 使用方式

每一項請依實際操作填入：

- 狀態：`待測` / `通過` / `失敗` / `需確認` / `跳過`
- 使用者回覆：記錄實測答案、錯誤訊息、畫面現象或操作結果。
- 證據 / 備註：可填截圖路徑、log 摘要、資料日期、輸出檔名或重現條件。

若遇到失敗，先記錄，不在本檔直接改程式。後續修復任務另開 scope。

## 本輪重點補充（2026-06-16）

本檔是主 UI 人工 smoke test 的單一入口；不要另建平行的 UI 手動驗證清單。專項 QA 報告只記錄特定分頁或特定驗證結論，跨工作區人工走查以本檔為準。

本輪數據更新相關人工驗證請特別確認：

1. 「快速更新（僅 SQLite）」與「安全更新（完整 CSV + SQLite）」會納入 TPEX official daily close quotes。
2. TPEX 日價成功時，CSV 寫入 `DATA_ROOT/daily_price_tpex/YYYYMMDD.csv`，SQLite 寫入 `daily_prices`；TWSE 仍寫入既有 `DATA_ROOT/daily_price/YYYYMMDD.csv` 與同一張 `daily_prices`。
3. TPEX endpoint timeout 或連線失敗時，UI 應以 warning 呈現並繼續其他資料同步，不應靜默失敗，也不應阻斷 TWSE、大盤、產業、券商分點與技術指標流程。
4. `broker_flows` 若仍是舊主鍵，券商分點同步前會先備份 DB，再受控升級為 `(分點名稱, 證券代號, 日期, trade_type)`，讓同一分點 / 股票 / 日期的 `買超` 與 `賣超` 可共存。
5. SQLite 資料檢視的單一日期預設今天，區間預設本月 1 日至今天，日期文字應完整可見；按「清除」後才不套用日期條件。
6. 查詢 `daily_prices` 的 `3207` 時，應可看到 TPEX 歷史資料已補入正式 SQLite；2026-06-17 排查結果為 `20140102..20260617`、共 2,907 筆。若只看到近兩日，代表 TPEX 歷史補齊或 SQLite 同步未完成，需重開數據更新 issue。
7. 表格不應再出現 `PandasTableModel.data 錯誤: The truth value of a Series is ambiguous`；若舊 schema 或 alias 造成重複欄名，UI 必須能正常顯示。

## 本輪重點補充（2026-06-18｜繼續全 UI 健檢）

本輪從 `c785ee3 Improve TPEX refresh workflow` 之後接續。上一輪已完成並 push 的重點：

1. TPEX 日價改走官方歷史查詢 endpoint，手動每日股價、快速更新與安全更新都應以區間流程補缺少 CSV，並同步 `daily_price/` + `daily_price_tpex/` 至 SQLite。
2. 每日股價手動下載完成後會自動同步 SQLite 並執行技術指標增量計算；若 TPEX 失敗，應以 warning 呈現，不應阻斷 TWSE 或後續同步。
3. 每日股價分頁新增「背景補齊 TPEX + 技術指標」與「檢查背景任務狀態」按鈕；背景狀態檔位於 `DATA_ROOT/meta_data/tpex_full_refresh_status.json`，本 repo 內 `meta_data/` 狀態檔與 log 屬本機輸出，不應提交。
4. 主視窗標題已改為 `baldr`，Application Manual 標題已同步。
5. 自動驗證已通過：`tests/test_ui_qt_update_view_workbench.py` 25 passed、`scripts/qa_validate_update_tab.py` 23 passed / 0 failed / 4 skipped、mypy 209 files success、changed Python files `py_compile` 通過。

本輪人工健檢建議順序：

1. 先重測「數據更新」中所有 `已修正待驗證` / `需確認` 項目，尤其 TPEX、SQLite Inspector、背景任務狀態與技術指標。
2. 若數據更新可接受，依序進入「市場觀察」→「每日決策」→「策略回測 / Research Lab」→「推薦分析」→「觀察清單」→「持倉管理」→「Runtime Observatory」。
3. 高風險操作（強制全量、清空、刪除正式紀錄、正式資料重建）只驗證取消 / 防呆 / 提示流程；若要實際執行，另開明確資料任務並先備份。
4. 每個失敗現象先記錄在本檔 Issue 記錄簿，不在本檔直接修程式；修復後狀態只能先改為 `已修正待驗證`，等使用者重新驗證後才改 `驗證通過`。

## Month 5 基本面資料層補充（2026-06-17）

### 已達成

1. `fundamental_monthly_revenues` 已正式寫入月營收 records：`2014-04..2026-05`、244,499 筆、1,848 檔、0 duplicate；2026-05 為 `observed`，歷史 baseline 為 `degraded`。
2. `fundamental_statement_items` 已正式寫入季度財報 item records：`2014-Q2..2024-Q1`、1,645,555 筆、1,567 檔、0 duplicate；income statement / balance sheet / cash flows 均可在 SQLite Inspector 檢查。
3. `fundamental_valuation_metrics` 已正式寫入 P/E records：831 筆，並保留 industry percentile。
4. `FundamentalSQLiteProvider` 與 `FundamentalFactorService` 已可唯讀產生基本面 factor records / diagnostics；不寫 DB、不接 `ScoringEngine`。
5. Revenue factors 已可產生 YoY、MoM、3M trend、new high。
6. Statement factors 已可產生 EPS、毛利率、營益率、ROE、業外損益 ratio。
7. PB / PS 已有明確 pending diagnostics，不會被 P/E 或 daily price 欄位臨時推導。

### 尚未達成 / 不在 Month 5 接入

1. 基本面 factor 不接 `ScoringEngine`，不影響推薦分數、排名、買賣判斷或回測交易邏輯。
2. PB / PS 尚未正式計算；P/B 仍需 book value per share 或 equity/share count 政策，P/S 仍需 market cap 與 TTM sales 政策。
3. 季度財報目前是 retroactive baseline，quality 為 `degraded`；不得用於導入日前的歷史回測假設。
4. AbnormalFundamentalFlag 目前只具備資料 foundation；是否進風險提示或 scoring 需另開 Month 6/後續政策。

### 新增人工檢查目標

1. SQLite 資料檢視下拉選單應可看到 `fundamental_monthly_revenues`、`fundamental_statement_items`、`fundamental_valuation_metrics`。
2. 查 `fundamental_monthly_revenues` / `2330` 應可看到 `2014-04..2026-05`；`2026-05` 為 `observed`，歷史月份為 `degraded`。
3. 查 `fundamental_statement_items` / `2330` / `2023-Q4` 應可看到 `EPS`、`Revenue`、`GrossProfit`、`OperatingIncome`、`Equity` 等 item。
4. 可執行：

```powershell
.\.venv\Scripts\python.exe scripts\inspect_fundamental_factors.py --all-monthly-revenue-stocks --decision-date 2026-06-30 --diagnostic-limit 12 --stock-summary-limit 12
.\.venv\Scripts\python.exe scripts\inspect_valuation_source_policy.py
```

第一個命令應顯示 `writes_data: false`、`scoring_engine_connected: false`，並列出 revenue / statement / valuation factor counts；第二個命令應顯示 P/E ready、P/B 與 P/S pending。

## 驗證總覽

| 工作區 | 子功能數 | 狀態 | 備註 |
|---|---:|---|---|
| 數據更新 | 29 | 已修正待驗證 | 使用者已完成第一輪人工驗證；長任務切換分頁中斷 / 卡住、SQLite 檢視日期輸入、簡體中文、漲跌欄位、排序需求、TPEX 歷史補齊、背景補齊按鈕與狀態查詢已修正或新增；Month 5 基本面表與 factor diagnostics 已加入待驗證。 |
| 市場觀察 | 22 | 待測 |  |
| 每日決策 | 9 | 待測 |  |
| 策略回測 / Research Lab | 42 | 待測 |  |
| 推薦分析 | 20 | 待測 |  |
| 觀察清單 | 13 | 待測 |  |
| 持倉管理 | 17 | 待測 |  |
| Runtime Observatory | 6 | 待測 |  |

## 數據更新

| ID | 分頁 / 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| U-001 | 全部資料 | 檢查數據狀態 | 5 張狀態卡更新最新日期、總筆數與燈號；日誌有檢查結果 | 通過 | 個別功能正常。 |  |
| U-002 | 全部資料 | 快速更新（僅 SQLite） | 下載近期缺失資料並同步 SQLite；進度條與日誌正常；不重寫大型 CSV；TPEX timeout 以 warning 呈現並繼續流程 | 已修正待驗證 | 檢查狀態、清除日誌、頁面說明正常；使用者追問快速更新是否可不更新大型 CSV 只更新 SQLite。 | 快速更新目前仍保留必要日檔 CSV 作為 SQLite 可追溯來源，但跳過 `stock_data_whole.csv` 與券商分點 `merged.csv` 大型重寫；2026-06-18 補充 Manual 說明。 |
| U-003 | 全部資料 | 安全更新（完整 CSV + SQLite） | 依序下載、合併 / 同步 CSV 與 SQLite、計算技術指標；失敗時可讀；TWSE + TPEX 日價同步到 `daily_prices` | 已修正待驗證 | 點更新後，若在尚未更新完成時切到其他 subtab，更新不會繼續跑。 | 已改為保留多個背景 worker，切換 subtab / 狀態查詢不再取消既有長任務；TPEX CSV 寫入 `DATA_ROOT/daily_price_tpex/`，TWSE CSV 保持 `DATA_ROOT/daily_price/`；待使用者重測。 |
| U-004 | 全部資料 | 清除日誌 | 日誌主控台可清空，不影響狀態卡 | 通過 | 清除日誌按鍵正常。 | 2026-06-18 使用者人工驗證。 |
| U-005 | 每日股價 | 日期範圍設定 | 結束日期與最近範圍可調整，切換分頁後日期同步；結束日期可用日曆選取並可一鍵回到今日 | 通過 | 2026-06-18 人工驗證：每日股價 / 大盤 / 產業 / 券商分點的「結束日期」旁已變成清楚的「日曆」按鈕。 | 2026-06-18 修正並驗證：保留跨資料源日期同步；結束日期新增「今日」與「日曆」按鈕，日曆按鈕取代原本不清楚的內建下拉。 |
| U-006 | 每日股價 | 檢查此資料源狀態 | 只更新每日股價狀態與日誌 | 待測 |  |  |
| U-007 | 每日股價 | 手動下載此資料源 | 原始資料下載成功；提示需合併才可供分析使用 | 待測 |  |  |
| U-008 | 每日股價 | 合併每日股價 | 增量合併並同步到 `daily_prices`；確認對話框正常；完成訊息最新日期需與實際合併結果一致 | 已修正待驗證 | 合併 CSV 尚未完成時切到其他 tab，任務會卡住；即使再執行匯出 CSV 且匯出完成，合併仍一直顯示「合併中」。2026-06-18 追加：使用者更正為「所有資料最新在 0618，但合併完成提示仍停在 0617」。 | 合併 worker 不再被切頁狀態查詢或 CSV 匯出覆蓋；完成 / error signal 會釋放各自 worker。2026-06-18 修正：`stock_data_whole.csv` 合併腳本同時納入 `daily_price/` 與 `daily_price_tpex/`，避免 TPEX 已有 0618 但大表只掃 TWSE 目錄而停在 0617。 |
| U-009 | 每日股價 | 匯出 CSV 備案 | 可選最近範圍 / 全部歷史；輸出 UTF-8 BOM CSV | 通過 | 匯出 CSV 可以完成。 | 但在合併卡住期間執行匯出，匯出完成不會解除合併中的錯誤狀態。 |
| U-010 | 每日股價 | 強制重新合併所有每日股價 | 有高風險確認；僅在明確同意後執行；不可破壞原始資料 | 待測 |  | 高風險，預設不測或只測取消流程 |
| U-011 | 大盤指數 | 日期範圍設定 | 結束日期與最近範圍可調整並同步 | 通過 | 大盤指數沒問題。 |  |
| U-012 | 大盤指數 | 檢查此資料源狀態 | 顯示大盤資料日期、筆數與日誌 | 通過 | 大盤指數沒問題。 |  |
| U-013 | 大盤指數 | 手動下載此資料源 | 大盤原始資料下載成功 | 通過 | 大盤指數沒問題。 |  |
| U-014 | 大盤指數 | 匯出 CSV 備案 | 匯出 `market_indices` 對應資料 | 通過 | 大盤指數沒問題。 |  |
| U-015 | 產業指數 | 日期範圍設定 | 結束日期與最近範圍可調整並同步 | 通過 | 產業指數沒問題。 |  |
| U-016 | 產業指數 | 檢查此資料源狀態 | 顯示產業資料日期、筆數與日誌 | 通過 | 產業指數沒問題。 |  |
| U-017 | 產業指數 | 手動下載此資料源 | 產業指數原始資料下載成功 | 通過 | 產業指數沒問題。 |  |
| U-018 | 產業指數 | 匯出 CSV 備案 | 匯出 `industry_indices` 對應資料 | 通過 | 產業指數沒問題。 |  |
| U-019 | 券商分點 | 日期範圍設定 | 結束日期與最近範圍可調整並同步 | 待測 |  |  |
| U-020 | 券商分點 | 檢查此資料源狀態 | 顯示券商分點資料日期、筆數與日誌 | 待測 |  |  |
| U-021 | 券商分點 | 手動下載此資料源 | MoneyDJ 分點資料下載成功 | 待測 |  |  |
| U-022 | 券商分點 | 合併券商分點 | 增量合併並同步到 `broker_flows`；保留 observed / estimated / unavailable 品質 | 已修正待驗證 | 合併券商分點未結束時不能跳其他 tab，否則會卡住。 | 合併 worker 不再被切頁狀態查詢覆蓋；待使用者重測。 |
| U-023 | 技術指標 | 計算技術指標 | 支援全部 / 單一股票、增量 / 全量；結果同步到 `technical_indicators` | 已修正待驗證 | 計算技術指標時出現大量 `[Registry] 舊版配置 ... 使用 legacy 預設值` 與「指標文件缺少日期欄位，將直接合併數據」訊息；單股檔疑似 3010 筆變 6021 筆。 | 2026-06-18 修正：legacy 預設提示每參數只記錄一次，後續降為 debug；技術指標合併會先把 `Date` 正規化為 `日期`，缺少日期時不再直接 concat；並修正同名欄位 canonicalization 誤刪 `日期` / `證券代號` 的問題。 |
| U-024 | SQLite 資料檢視 | 載入、篩選、分頁、Schema | 唯讀查詢、頁碼控制、篩選重設、stale 結果防護、Schema 顯示正常；日期預設空白但打開日曆預設今天；券商分點可由下拉選單選取 | 通過 | 2026-06-18 人工驗證：三個日期欄位預設空白；日期按「日曆」會跳到今天月份；選 `broker_flows` 後，券商分點旁按「展開」可看到下拉清單。 | 已加入日曆日期 picker、繁中欄位 alias、新建 schema 繁中 `漲跌`、`漲跌價差` 依 `漲跌(+/-)` 顯示正負、表頭 server-side ORDER BY 排序；表格以 column position 取值以避免重複欄名造成 Series ambiguity。2026-06-18 修正並驗證：日期預設空白、日曆定位今天月份、單一日期新增「今日」、區間旁單一「清除」會清掉所有日期，`broker_flows` 券商分點改為可輸入可選擇並可展開的下拉。 |
| U-025 | 月營收 | MOPS snapshot 月營收正式資料 | 月營收分頁三個欄位為中文；可先檢查再寫入；正式 DB 已有 `fundamental_monthly_revenues` 244,499 筆 | 已修正待驗證 | Month 5 已正式寫入月營收與歷史 baseline。 | 使用 SQLite Inspector 查 `fundamental_monthly_revenues`，確認 1,848 檔、`2014-04..2026-05`、0 duplicate。 |
| U-026 | SQLite 資料檢視 | 基本面資料表檢視 | 下拉選單可檢視三張 fundamental tables；股票代號與日期篩選可用 | 已修正待驗證 | 之前 SQLite 資料檢視層看不到新的 table。 | 白名單已加入 `fundamental_monthly_revenues`、`fundamental_statement_items`、`fundamental_valuation_metrics`；待人工檢查。 |
| U-027 | 基本面 factor diagnostics | Month 5 factor inspection CLI | 可檢查 revenue / statement / valuation factor records；不寫資料、不接 ScoringEngine | 需確認 | Month 5 基本面不接 scoring，但要能檢查 diagnostics。 | 執行 `scripts\inspect_fundamental_factors.py` 與 `scripts\inspect_valuation_source_policy.py`；確認 PB/PS pending diagnostics。 |
| U-028 | 每日股價 | 背景補齊 TPEX + 技術指標 | 點擊後 UI 不阻塞；不重複啟動第二個背景任務；狀態檔寫入 running / done / failed；不先強制跑 TWSE 全量；技術指標若已與每日股價同日則跳過 | 已修正待驗證 | 背景任務曾先卡在 TWSE 全量，需改為先補 TPEX / SQLite / 技術指標。2026-06-18 追加：狀態顯示 technical running，但資料似乎已存在，懷疑背景任務未先檢查就重算。 | UI 已新增背景按鈕；背景腳本預設不傳 `--twse-update`，TPEX `force_refresh=False`，狀態檔不提交。2026-06-18 修正：移除 UI 背景任務的 `--technical-force-all`，背景腳本先比對 `daily_data.latest_date` 與 `technical_indicators.latest_date`，已最新時直接 skipped；落後時使用 120 天 warmup。 |
| U-029 | 每日股價 | 檢查背景任務狀態 | 可讀取 `tpex_full_refresh_status.json` 並顯示 TWSE / TPEX / SQLite / 技術指標四步狀態；done / failed 後按鈕恢復 | 已修正待驗證 | 需要能確認背景任務是否真的還在跑、成功或失敗。 | 點「檢查背景任務狀態」應在 console 與訊息框顯示狀態、rows 與 message；若無狀態檔，顯示尚未啟動。 |

### 數據更新追加 Issue Record（2026-06-17）

| ID | 來源 | 狀態 | 使用者回覆 / 現象 | 證據 / 備註 |
|---|---|---|---|---|
| U-I-20260617-001 | 每日股價 / TPEX / SQLite | 已修正待驗證 | `3207` 在 SQLite `daily_prices` 只看到最近兩天，懷疑 TPEX 開始有資料但未補歷史。 | 盤點結果：TWSE CSV 已有 `2014-01-02..2026-06-17`；TPEX CSV 原僅 `2026-06-16..2026-06-17`。SQLite 中 `3207` 原為 2 筆；手動補 `2026-06-15` 後為 3 筆。 |
| U-I-20260617-002 | 每日股價 / 手動下載此資料源 | 已修正待驗證 | 選 10 天時沒有明顯跑 TPEX，顯示沒有需要更新。 | 根因：舊 TPEX source 使用 OpenAPI latest endpoint，歷史日期參數未有效回傳指定日期；已改用官方 `/www/zh-tw/afterTrading/otc` JSON 歷史查詢 endpoint，並改為只補缺少 CSV。 |
| U-I-20260617-003 | 全部資料 / 快速更新 | 已修正待驗證 | 快速更新與安全更新差異不清楚，需確認 TWSE + TPEX 是否都跑。 | 快速更新改為 TWSE 每日股價、TPEX 每日股價與券商分點皆只更新結束日前最近 2 天，直接同步 SQLite 並跳過大型合併；安全更新依 UI 日期範圍補齊並執行完整合併。 |
| U-I-20260617-004 | 背景補齊 TPEX + 技術指標 | 已修正待驗證 | 背景流程不應先卡在 TWSE 全量，應先看 SQLite / CSV 缺口再補 TPEX。 | UI 啟動背景腳本時不再傳 `--twse-update`；背景腳本 TPEX 改為 `force_refresh=False`，並以狀態檔回報 TWSE skipped / TPEX / SQLite / technical steps。 |
| U-I-20260617-005 | 每日股價 / SQLite sync | 已修正待驗證 | TPEX CSV 有資料但 SQLite sync 可能因欄位名稱混亂失敗。 | 2026-06-15 TPEX CSV 抓取 879 筆；初次 sync 因 mojibake 欄位判斷失敗，已將 `daily_price_files` sync 正規化到 `日期`、`證券代號`，重跑後該日同步 1,969 筆。 |
| U-I-20260618-001 | Git / QA 母檔接續 | 已記錄 | 接下來要開始繼續全 UI 健檢，需要明確接續點。 | 已補本輪 2026-06-18 重點、建議健檢順序、驗證證據與本機 `meta_data/` 排除提醒。 |

## 市場觀察

| ID | 子分頁 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| M-001 | 大盤指數 | 檢測市場狀態 | Regime、信心度、摘要與策略建議可顯示 | 待測 |  |  |
| M-002 | 大盤指數 | 技術細節顯示 | 價格 / 均線 / 趨勢 / 評分 / 判斷條件內容合理 | 待測 |  |  |
| M-003 | 強勢個股 | 本日 / 本周切換 | 期間切換後資料與標籤一致 | 待測 |  |  |
| M-004 | 強勢個股 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 待測 |  |  |
| M-005 | 強勢個股 | 刷新 | 重新計算 / 重新載入後表格更新 | 待測 |  |  |
| M-006 | 強勢個股 | 加入觀察清單 | 選取股票可加入候選池，重複加入有合理處理 | 待測 |  |  |
| M-007 | 弱勢個股 | 本日 / 本周切換 | 期間切換後資料與標籤一致 | 待測 |  |  |
| M-008 | 弱勢個股 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 待測 |  |  |
| M-009 | 弱勢個股 | 刷新 | 重新計算 / 重新載入後表格更新 | 待測 |  |  |
| M-010 | 弱勢個股 | 加入觀察清單 | 選取股票可加入候選池 | 待測 |  |  |
| M-011 | 強勢產業 | 本日 / 本周切換 | 期間切換後資料與排序合理 | 待測 |  |  |
| M-012 | 強勢產業 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 待測 |  |  |
| M-013 | 強勢產業 | 刷新 | 重新計算 / 重新載入後表格更新 | 待測 |  |  |
| M-014 | 弱勢產業 | 本日 / 本周切換 | 期間切換後資料與排序合理 | 待測 |  |  |
| M-015 | 弱勢產業 | 載入數據 | 第一次載入不報錯，表格有資料或明確空狀態 | 待測 |  |  |
| M-016 | 弱勢產業 | 刷新 | 重新計算 / 重新載入後表格更新 | 待測 |  |  |
| M-017 | 主力流向 | 週期切換 | 日線 / 週線 / 月線切換後統計一致 | 待測 |  |  |
| M-018 | 主力流向 | 圖表型態切換 | 直方 / 折線 / 面積圖切換正常 | 待測 |  |  |
| M-019 | 主力流向 | 開始掃描 | 主表、摘要卡、趨勢圖與右側明細可更新 | 待測 |  |  |
| M-020 | 主力流向 | 表格排序與懸停提示 | 點表頭可排序；近期 5 筆 tooltip 正常 | 待測 |  |  |
| M-021 | 主力流向 | 加入觀察清單 | 選取個股可加入候選池 | 待測 |  |  |
| M-022 | 主力流向 / 分點進出追蹤 | 選擇券商分點 | 分點操作股票、張數、標籤與趨勢可顯示 | 待測 |  |  |

## 每日決策

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| D-001 | 頂層頁 | 初始化 | 分頁可開啟；若 provider 初始化失敗，有可讀 fallback | 待測 |  |  |
| D-002 | 頂層頁 | 更新決策摘要 | Snapshot 可重建，失敗時不讓整個 App 中斷 | 待測 |  |  |
| D-003 | 摘要 | as_of_date / quality / warnings | 日期、整體品質與警告彙總正確顯示 | 待測 |  |  |
| D-004 | Market Regime | 市場狀態摘要 | Regime label、score、confidence 與品質狀態合理 | 待測 |  |  |
| D-005 | Market Breadth | 市場寬度 | 多方 / 空方 / 持平、新高新低、成交量擴散與 fallback warning | 待測 |  |  |
| D-006 | Sector Rotation | 產業輪動 | 領先 / 落後產業、5 / 20 日變化、輪動強度與 warning | 待測 |  |  |
| D-007 | Relative Strength / Liquidity | 強弱與流動性 | 強勢、弱勢、低流動性代碼清單與歷史不足降級 | 待測 |  |  |
| D-008 | Watchlist Trigger | 候選池觸發 | score_bp、risk_alert、新進 / 強度提升 / 下降統計 | 待測 |  |  |
| D-009 | Portfolio Alert / Why Not | 持倉警示與風險提示 | condition、chip risk、來源歸因、低流動性 / 弱勢 / watchlist / portfolio 提示 | 待測 |  |  |

## 策略回測 / Research Lab

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| B-001 | 實驗模式 | 單股回測模式 | 模式切換後欄位、按鈕與說明一致 | 待測 |  |  |
| B-002 | 實驗模式 | 批次股票回測模式 | 可從選股清單載入多檔並進入批次結果 | 待測 |  |  |
| B-003 | 實驗模式 | 固定組合回測模式 | 固定組合 metadata 與 per-stock 保存提示一致 | 待測 |  |  |
| B-004 | 實驗模式 | 推薦系統回放模式 | 推薦回放設定區可用；執行入口正常 | 待測 |  |  |
| B-005 | 實驗模式 | 策略研究模式 | 策略研究相關 UI 顯示正常 | 待測 |  |  |
| B-006 | 策略來源 / 預設 | 儲存 Preset | 可保存目前策略 / 參數設定 | 待測 |  |  |
| B-007 | 策略來源 / 預設 | 載入 Preset | 載入後 UI 控制項與策略設定同步 | 待測 |  |  |
| B-008 | 策略來源 / 預設 | 刪除 Preset | 有確認流程；刪除後清單更新 | 待測 |  |  |
| B-009 | 輸入來源 | 單一股票 | 股票代號輸入與日期範圍可用 | 待測 |  |  |
| B-010 | 輸入來源 | 選股清單 | 下拉清單可載入 Universe；管理按鈕可開啟清單管理 | 待測 |  |  |
| B-011 | 選股清單管理 | 新增 / 編輯 / 刪除 / 關閉 | 對話框操作與資料同步正常 | 待測 |  |  |
| B-012 | 資金成本 / 執行 | 初始資金 | 範圍、格式、整股限制提示合理 | 待測 |  |  |
| B-013 | 資金成本 / 執行 | 手續費 bps / 滑價 bps | 可輸入並進入回測 metadata / 報告 | 待測 |  |  |
| B-014 | 資金成本 / 執行 | 成交價假設 | `next_open` / `close` 切換，結果揭露一致 | 待測 |  |  |
| B-015 | 停損停利 | 百分比模式 | 停損 / 停利百分比生效 | 待測 |  |  |
| B-016 | 停損停利 | ATR 倍數模式 | ATR 停損 / 停利倍數生效 | 待測 |  |  |
| B-017 | 部位 Sizing | 全倉 | 部位計算符合整股與資金限制 | 待測 |  |  |
| B-018 | 部位 Sizing | 固定金額 | 每次固定金額設定生效 | 待測 |  |  |
| B-019 | 部位 Sizing | 風險百分比 | 風險比例與 ATR 互動正確 | 待測 |  |  |
| B-020 | 部位管理 | 最大持倉數 | 批次 / 組合模式下限制生效 | 待測 |  |  |
| B-021 | 部位管理 | 等權 / 分數加權 / 波動調整 | 權重模式切換不報錯，metadata 記錄正確 | 待測 |  |  |
| B-022 | 部位管理 | 允許加碼 | 勾選後加碼邏輯與結果可辨識 | 待測 |  |  |
| B-023 | 部位管理 | 允許重新進場 / 冷卻期 | 冷卻期與重進場限制生效 | 待測 |  |  |
| B-024 | 市場限制 | 漲跌停限制 | 限制開關影響成交與未成交原因 | 待測 |  |  |
| B-025 | 市場限制 | 成交量約束 / 最大參與率 | 低流動性或參與率超限時有合理拒絕 / 標記 | 待測 |  |  |
| B-026 | 策略配置 | 策略下拉與參數 | 策略切換後參數與說明更新 | 待測 |  |  |
| B-027 | 進階驗證 | 參數最佳化 | 可執行參數掃描，目標包含 Sharpe、年化報酬、CAGR-MDD | 待測 |  |  |
| B-028 | 進階驗證 | 套用選中參數 | 最佳化結果可套回設定 | 待測 |  |  |
| B-029 | 進階驗證 | Train-Test Split | 可執行驗證並顯示 OOS 結果 | 待測 |  |  |
| B-030 | 進階驗證 | Walk-forward | 訓練月數、測試月數、步進月份生效 | 待測 |  |  |
| B-031 | 推薦回放 | Top N / 候選上限 / 持有天數 | 推薦回放參數進入執行快照 | 待測 |  |  |
| B-032 | 推薦回放 | 重播頻率 / 資金配置 | 每週重播 / 只跑一次、等權 / 分數加權正常 | 待測 |  |  |
| B-033 | 執行與下一步 | 執行實驗 | 背景執行、進度、按鈕鎖定與完成狀態正常 | 待測 |  |  |
| B-034 | 執行與下一步 | 取消執行 | 合作式取消後 UI 解鎖，不產生新舊任務重疊 | 待測 |  |  |
| B-035 | 執行與下一步 | 保存結果 | 成功結果可保存到 Research Run Registry；stale result 不可保存 | 待測 |  |  |
| B-036 | 執行與下一步 | 升級為策略版本 | Registry Gate、最低樣本與完整性限制生效 | 待測 |  |  |
| B-037 | 結果 / 實驗摘要 | 匯出 Excel 報告 | 單股成功後啟用；背景原子寫入；資料完整性區塊正確 | 待測 |  |  |
| B-038 | 結果 / 圖表 | 權益、回撤、報酬分佈、持有天數 | fast renderer / fallback 顯示正常 | 待測 |  |  |
| B-039 | 結果 / 歷史與比較 | 重新整理 / 刪除選中 / 比較選中 | legacy run 操作正常，有確認與錯誤處理 | 待測 |  |  |
| B-040 | 結果 / 批次結果 | 排行榜、雙擊明細、匯出批次 Excel | 批次結果可讀、雙擊載入明細、匯出成功 | 待測 |  |  |
| B-041 | 結果 / 推薦回放 | 保存 / 刪除 / 升級 / 匯出回放 Excel | 回放結果、credibility、unfilled、cash、weight、gap 顯示與操作正常 | 待測 |  |  |
| B-042 | 結果 / Registry 比較 | 篩選、分頁、選取 2-5 run、比較 | comparability badge、參數差異、normalized equity、metrics 與 benchmark 顯示 | 待測 |  |  |

## 推薦分析

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| R-001 | 模式 | 新手 / 進階模式切換 | 切換文字、欄位顯示與設定保留合理 | 待測 |  |  |
| R-002 | 市場狀態 | Regime 顯示 / 自動偵測 | 市場狀態可讀，錯誤時有提示 | 待測 |  |  |
| R-003 | 策略建議 | 一鍵套用建議 Profile | 套用後 Profile 與參數有變更且可辨識 | 待測 |  |  |
| R-004 | Profile | 選擇策略風格 | Profile 下拉可用，切換後設定同步 | 待測 |  |  |
| R-005 | 策略傾向 | 偏好設定 | 設定變更會影響推薦配置 | 待測 |  |  |
| R-006 | 技術指標 | 指標勾選 / 參數 | 啟用指標需完整參數；錯誤參數 fail-closed | 待測 |  |  |
| R-007 | 圖形模式 | 模式勾選 / 參數 | 圖形條件可配置，不產生未來函數假象 | 待測 |  |  |
| R-008 | 篩選條件 | 最小漲幅 | 篩選條件進入推薦服務 | 待測 |  |  |
| R-009 | 篩選條件 | 最小成交量比率 | 篩選條件進入推薦服務 | 待測 |  |  |
| R-010 | 篩選條件 | 產業篩選 | 產業清單可載入，全部 / 特定產業正常 | 待測 |  |  |
| R-011 | 排名門檻 | 固定門檻 | fixed 模式結果與 metadata 可辨識 | 待測 |  |  |
| R-012 | 排名門檻 | 百分位排名 | quantile 模式需母體、百分位與 method metadata；母體過小有錯誤提示 | 待測 |  |  |
| R-013 | 執行 | 執行推薦分析 | 背景執行、進度條、結果表與日誌正常 | 待測 |  |  |
| R-014 | 結果 | 推薦理由詳情 | Why / Why Not / Explain 與分數拆解可讀 | 待測 |  |  |
| R-015 | 結果 | 保存結果 | 保存推薦配置、Profile、Regime 與名單 | 待測 |  |  |
| R-016 | 結果 | 加入候選池 | 選取股票可加入 Watchlist | 待測 |  |  |
| R-017 | 結果 | 送 Research Lab 批次回測 | 切換到策略回測並帶入股票 / 配置 | 待測 |  |  |
| R-018 | 結果 | 送 Research Lab 推薦回放 | 切換到策略回測並帶入推薦回放配置 | 待測 |  |  |
| R-019 | 結果 | 匯出 Excel | 背景匯出，包含 metadata、配置、Regime 與推薦清單 | 待測 |  |  |
| R-020 | 結果右鍵 | 記錄到持倉 / 加入候選池 | 右鍵選單可用且保留推薦來源 metadata | 待測 |  |  |

## 觀察清單

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| W-001 | 候選池 | 刷新 | 候選池重新載入正常 | 待測 |  |  |
| W-002 | 候選池 | 新增股票 | 手動輸入股票成功，資料欄位合理 | 待測 |  |  |
| W-003 | 候選池 | 移除選中 | 選取列可移除，有合理提示 | 待測 |  |  |
| W-004 | 候選池 | 清空候選池 | 有不可逆確認；清空後畫面更新 | 待測 |  |  |
| W-005 | 候選池 | 送 Research Lab 批次回測 | 目前若停用，需確認按鈕狀態與提示符合 Manual | 待測 |  |  |
| W-006 | 候選池 | 保存為選股清單 | 可將候選池保存為 Universe | 待測 |  |  |
| W-007 | 候選池右鍵 | 移除選取 | 右鍵移除與按鈕行為一致 | 待測 |  |  |
| W-008 | 選股清單 | 刷新 | Universe 清單重新載入正常 | 待測 |  |  |
| W-009 | 選股清單 | 載入到候選池 | 既有 Universe 可載入候選池 | 待測 |  |  |
| W-010 | 選股清單 | 新增 | 可建立 Universe 名稱、說明與股票內容 | 待測 |  |  |
| W-011 | 選股清單 | 編輯 | 可修改 Universe metadata / 股票清單 | 待測 |  |  |
| W-012 | 選股清單 | 刪除 | 有確認；刪除後清單更新 | 待測 |  |  |
| W-013 | 跨頁同步 | 從市場觀察 / 推薦加入後顯示 | 其他頁加入候選池後，切回觀察清單可看到 | 待測 |  |  |

## 持倉管理

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| P-001 | 主操作 | 手動記錄交易 | 買 / 賣、價格、股數、日期、費用、稅金與來源欄位可保存 | 待測 |  |  |
| P-002 | 主操作 | 新增日記 | 選取持倉後可新增覆盤日誌 | 待測 |  |  |
| P-003 | 主操作 | 整理刷新 | 持倉、交易、日誌與監控重新整理 | 待測 |  |  |
| P-004 | 主操作 | 清空全體數據 | 二次確認；預設只測取消流程，避免破壞資料 | 待測 |  | 高風險 |
| P-005 | 活躍持倉 | 持倉列表 | 平均成本、投入資金、目前狀態與來源摘要正確 | 待測 |  |  |
| P-006 | 活躍持倉右鍵 | 為此部位寫日記 | 右鍵建立日記並帶入股票 | 待測 |  |  |
| P-007 | 活躍持倉右鍵 | 只查看此股交易歷史 | 交易歷史可依股票過濾 | 待測 |  |  |
| P-008 | 活躍持倉右鍵 | 顯示全部交易歷史 | 清除過濾正常 | 待測 |  |  |
| P-009 | 交易歷史 | 檢視交易明細 | 選取持倉後交易歷史同步 | 待測 |  |  |
| P-010 | 交易歷史右鍵 | 刪除此交易紀錄 | 有確認；刪除後持倉與平均成本重算 | 待測 |  |  |
| P-011 | 覆盤日誌 | 檢視日誌 | 選取持倉後日誌同步 | 待測 |  |  |
| P-012 | 覆盤日誌右鍵 | 刪除此篇日記筆記 | 有確認；刪除後列表更新 | 待測 |  |  |
| P-013 | 策略與價格監控 | 目前價格 / 未實現損益 | 價格、PnL、停損停利與狀態原因顯示 | 待測 |  |  |
| P-014 | 策略與價格監控 | 來源追溯 | 推薦 / 回測 / 策略版本來源 metadata 可讀 | 待測 |  |  |
| P-015 | 籌碼監控 | 籌碼風險與分點明細 | chip risk、近期分點買賣與品質狀態顯示 | 待測 |  |  |
| P-016 | 籌碼監控 | 下鑽詳細主力流向 | 可切到市場觀察 > 主力流向並定位該股票 | 待測 |  |  |
| P-017 | 跨頁來源 | 從推薦 / 回測記錄到持倉 | 來源 metadata 保留，且不自動下單 | 待測 |  |  |

## Runtime Observatory

| ID | 區域 | 功能 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|---|
| RT-001 | 初始化 | 分頁載入 | Runtime 初始化成功；失敗不影響其他分頁 | 待測 |  |  |
| RT-002 | Objective | 目前任務目標 | Objective 欄位可顯示或明確空狀態 | 待測 |  |  |
| RT-003 | Task Workflow Status | FSM 狀態 | 狀態更新隨 timer poll 變化 | 待測 |  |  |
| RT-004 | Overall System State / Health | 系統健康 | 健康資訊、拒絕率、錯誤率可讀 | 待測 |  |  |
| RT-005 | Last Critical Violation | 最近重大違規 | 無事件時有空狀態；有事件時可讀 | 待測 |  |  |
| RT-006 | Append-only Event Stream | 事件流 | 時間、嚴重度、actor、event type、訊息正常追加 | 待測 |  |  |

## 全域與跨工作區流程

| ID | 流程 | 驗證重點 | 狀態 | 使用者回覆 | 證據 / 備註 |
|---|---|---|---|---|---|
| X-001 | 啟動主程式 | `ui_qt/main.py` 可啟動；主視窗、樣式、icon、status bar 正常 | 待測 |  |  |
| X-002 | Tab 順序 | 數據更新、市場觀察、每日決策、策略回測、推薦分析、觀察清單、持倉管理、Runtime Observatory 順序符合目前程式 | 待測 |  |  |
| X-003 | 市場觀察自動載入 | 切換到市場觀察子頁時，強弱股 / 產業 / 主力流向會需要時載入 | 待測 |  |  |
| X-004 | 推薦到回測 | 推薦分析可把批次 / 推薦回放配置送到 Research Lab | 待測 |  |  |
| X-005 | 推薦 / 回測到持倉 | 右鍵記錄交易可保留來源追溯 | 待測 |  |  |
| X-006 | 持倉到主力流向 | 籌碼監控下鑽可切頁並定位股票 | 待測 |  |  |
| X-007 | Session context strip | 狀態列研究 session context 顯示不遮擋、不報錯 | 待測 |  |  |

## 待後續整理的失敗紀錄

### Issue 狀態規則

後續修復時，本檔採用 issue 記錄簿模式：

| 狀態 | 意義 |
|---|---|
| 已記錄 | 已由人工驗證發現並寫入本檔，尚未修正。 |
| 修正中 | 已開始分析或修改，但尚未完成。 |
| 已修正待驗證 | 程式已修正，但尚未由使用者重新操作驗證。 |
| 驗證通過 | 使用者已重新驗證，確認問題消失。 |
| 重開 | 修正後仍重現，或衍生出同一根因問題。 |

原則：修正完成後只能先標示為「已修正待驗證」，不得直接宣稱完成；必須等使用者再次驗證後，才能改成「驗證通過」。

### 數據更新 Tab Issue 記錄簿

| Issue ID | 來源功能 ID | 問題分類 | 問題摘要 | 目前狀態 | 修正狀態 | 驗證狀態 | 優先級 | 後續處理 |
|---|---|---|---|---|---|---|---|---|
| UPDATE-ISSUE-001 | U-002 / U-003 | 背景任務 / 分頁切換 | 全部資料更新中切換 subtab 後不再繼續。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | UpdateView 改為追蹤多個 active workers；狀態查詢 / 切頁不再取消既有更新 worker。 |
| UPDATE-ISSUE-002 | U-008 | 背景任務 / UI 狀態 | 每日股價合併 CSV 中切 tab 後卡住，後續匯出 CSV 完成也不會解除「合併中」。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 合併與匯出各自保留 worker reference；完成 / error signal 後釋放自身 worker，避免互相覆蓋。 |
| UPDATE-ISSUE-003 | U-022 | 背景任務 / 分頁切換 | 券商分點合併中切 tab 後卡住。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 券商分點合併 worker 不再被切頁觸發的狀態查詢覆蓋。 |
| UPDATE-ISSUE-004 | U-023 | 背景任務 / 分頁切換 | 技術指標計算中切 tab 後中斷，不會背景繼續。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 技術指標計算 worker 改由 active worker registry 保留到任務完成。 |
| UPDATE-ISSUE-005 | U-024 | SQLite 檢視 UX | SQLite 檢視單一日期與日期區間只能手打，希望改為可點選日期 / 日曆。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：SQLite 三個日期欄位預設空白，按「日曆」會跳到今天月份。 |
| UPDATE-ISSUE-006 | U-024 | 語言 / 欄位顯示 | SQLite 檢視 `daily_price` / `daily_prices` 顯示簡體中文欄位「漲跌」。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 新建 daily_prices schema 改為繁中 `漲跌`；舊 DB 若仍有 `涨跌`，Inspector 顯示 alias 為 `漲跌`。 |
| UPDATE-ISSUE-007 | U-024 / U-023 | 資料語意 / 技術指標可信度 | 漲跌欄位空白，漲跌價差全為正且整欄綠色，方向似乎另在 `漲跌(+/-)`；連帶懷疑 technical indicators 是否用錯誤數據計算。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | Inspector 的 `漲跌價差` 依 `漲跌(+/-)` / `漲跌` 方向顯示正負；技術指標計算路徑使用 OHLC 價格欄位，不直接以 `漲跌價差` 作為指標計算基礎。 |
| UPDATE-ISSUE-008 | U-024 | SQLite 檢視 UX / 效能 | SQLite 檢視所有欄位不能排序。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 表頭點擊會以白名單欄位送出 server-side `ORDER BY`，沿用既有分頁查詢避免 UI 端全量排序。 |
| UPDATE-ISSUE-009 | U-002 / U-003 | TPEX 更新韌性 | TPEX official endpoint timeout 會讓快速更新顯示失敗。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | TPEX 失敗改為 workflow warning，其他資料源繼續同步；日誌與完成視窗顯示 TPEX 診斷。 |
| UPDATE-ISSUE-010 | U-024 | SQLite 檢視 UX | SQLite 檢視日期預設為 1900，且欄位寬度不足，需要手動拉大 UI。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：SQLite 三個日期欄位不再顯示 `1900-01-01`，預設為空白；日曆開啟時定位今天月份。 |
| UPDATE-ISSUE-011 | U-024 | SQLite 表格顯示 | 查詢 `3207` 時，PandasTableModel 因重複欄名 alias 取到 Series，出現 truth value ambiguous 錯誤。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 表格 display data 改以欄位位置取值，避免重複顯示名稱造成 Series ambiguity。 |
| UPDATE-ISSUE-012 | U-022 | SQLite 同步 / 唯一鍵 | 券商分點同一分點 / 股票 / 日期同時存在 `買超` 與 `賣超` 時，舊唯一鍵造成衝突。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | broker sync 前受控備份並升級 `broker_flows` 主鍵納入 `trade_type`；合併去重鍵也納入 `trade_type`。 |
| UPDATE-ISSUE-013 | U-021 / U-022 | 效能 / 券商分點更新 | 券商分點資訊一次更新約 40 個分點時耗時明顯過長，需評估是否可支援受控並行，例如 5、10 或更高並行數，並確認網頁端是否會阻擋。 | 已記錄 | 未修正 | 待排查 | 中 | 先排查目前券商分點下載 / 合併是否為序列流程、是否已有 rate limit / retry / session 限制；後續若可行，需採受控 concurrency 與保守預設。 |
| UPDATE-ISSUE-014 | U-023 | 效能 / 技術指標計算 | 技術指標目前疑似 inline / 單核心計算；需評估是否可用 4 core 等多核心並行計算以縮短全市場計算時間。 | 已記錄 | 未修正 | 待排查 | 中 | 先排查目前計算流程是否逐股序列、是否受 SQLite 寫入或 CSV I/O 限制；若要並行，需拆分計算與寫入階段，避免多進程同時寫 DB 或覆寫 CSV。 |
| UPDATE-ISSUE-015 | U-024 / U-007 / U-008 | TPEX / SQLite 同步缺口 | SQLite 檢視 `3207` 只有近兩天資料，顯示 TPEX 日常資料已開始寫入但尚未補齊 2014 起歷史資料；需確認歷史 TPEX 回補是否未執行。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | 已改用 TPEX historical afterTrading endpoint 回補並同步 SQLite；2026-06-17 排查確認 `daily_prices` 中 `3207` 已有 `20140102` 至 `20260617`、共 2907 筆。 |
| UPDATE-ISSUE-016 | U-007 / U-008 / U-024 | TPEX / 每日股價手動同步 | 點每日股價的手動下載資料源或後續同步後，SQLite 檢視 `daily_prices` 查不到 `3207`，但 TPEX CSV 未消失；疑似手動每日股價路徑未納入 `daily_price_tpex`，或同步時覆蓋了同日 TPEX rows。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 每日股價手動下載改為 TWSE + TPEX 區間流程，完成後同步 `daily_price_files` 到 SQLite 並執行技術指標增量；測試替身保留舊單日 API fallback。 |
| UPDATE-ISSUE-017 | U-002 / U-003 | 使用者理解 / 更新模式語意 | 快速更新與完整更新差異不清楚：快速更新後 metadata 的市場 / 產業 CSV、`daily_price` 與 `broker_flow` daily 檔也會更新；完整更新又重跑一次全部，使用者不確定完整更新額外做了哪些檢查或資料重建。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 中 | UI tooltip 與 Manual 已更新：快速更新只跑結束日前最近 2 天並跳過大型合併；安全更新依 UI 日期範圍補齊並重建完整 CSV + SQLite。 |
| UPDATE-ISSUE-018 | U-023 / U-024 | 技術指標 / SQLite 欄位相容性 | TPEX 歷史 daily 寫入 SQLite 後，全量技術指標背景計算失敗，訊息為「沒有有效的數據可以合併」；`daily_prices` 已有 `3207` 歷史資料，但 `technical_indicators` 原本沒有 `3207`。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 根因為技術指標合併與 SQLite 寫入仍檢查舊 mojibake 欄名，且 `daily_prices` 同時存在正確欄名與舊 alias 欄位，rename 後造成 duplicate `日期`；已修正欄位 canonicalization，正式狀態檢查顯示 `technical_indicators` 最新日期 `2026-06-17`、總筆數 5,126,249。 |
| UPDATE-ISSUE-019 | U-001 / U-024 | 資料狀態 / SQLite 欄位相容性 | 開啟 App 檢查資料狀態時，`market_indices`、`industry_indices`、`broker_flows`、`technical_indicators` 狀態查詢仍使用舊 mojibake 欄名，也就是 `日期`、`證券代號` 等欄位被錯誤編碼後的版本，導致 no such column error。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | `UpdateService` 的 SQLite status helper 改為依實際 table columns 選擇 canonical 欄位，優先使用 `日期`、`證券代號`、`分點名稱`，必要時才 fallback 舊 alias；避免資料狀態頁因 schema canonicalization 後查錯欄位。 |
| UPDATE-ISSUE-020 | U-001 / U-024 | 文字編碼 / DB schema 清理 | App log、狀態檔與 `daily_prices` schema 殘留 mojibake 文字，造成使用者看到不可讀訊息，且可能讓後續程式誤用舊欄位。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 已修復 `app_module/update_service.py` 內正式路徑可見亂碼字串與註解，修復 `meta_data` 狀態 JSON 舊訊息；SQLite `daily_prices` 的空白亂碼欄位已在備份 `D:/Min/Python/Project/FA_Data/sqlite/twstock_before_drop_mojibake_col_20260617_235005.db` 後移除，最終 schema 掃描未再發現 mojibake 欄位。 |
| UPDATE-ISSUE-021 | U-021 / U-022 | 效能 / 券商分點跳過判斷 | 快速更新券商分點約 5 分鐘，明明近兩天才更新過，不應重新抓取所有既有資料。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | 根因之一是 SQLite 既有資料檢查用 `分點名稱 = branch_system_key`，但 DB 實際常存顯示名稱；已改為同時比對分點顯示名稱與 system key，已有資料時不啟動 MoneyDJ 頁面抓取。受控並行仍列在 UPDATE-ISSUE-013 待排查。 |
| UPDATE-ISSUE-022 | U-023 | 技術指標 / CSV 增量合併 | 技術指標計算日誌出現大量「缺少日期欄位，將直接合併數據」，多個單股指標檔筆數倍增。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | `calculate_and_store_indicators()` 會先將新結果 `Date` 正規化為 `日期`；若舊檔或新結果仍缺少可辨識日期，改為覆蓋新結果而非直接 concat。`UpdateService.calculate_technical_indicators()` 也修正同名欄位 canonicalization 誤刪 `日期` / `證券代號`。 |
| UPDATE-ISSUE-023 | U-001 / U-025 | 全部資料看板 / 月營收狀態 | 「全部資料」看板目前沒有月營收狀態卡，使用者看不到月營收最新日期、筆數與狀態。 | 已記錄 | 未修正 | 待處理 | 中 | 2026-06-18 使用者人工驗證發現；後續應評估在全部資料看板新增月營收卡片，或在狀態卡區加入基本面 / 月營收 summary。 |
| UPDATE-ISSUE-024 | U-005 | 每日股價日期 UX | 手動下載日期範圍在多個子 tab 同步，但結束日期的日曆圖示不清楚，缺少一鍵今日，日曆開啟時需預設今天且日期格不能顯示成 `...`。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：每日股價 / 大盤 / 產業 / 券商分點的「結束日期」旁已是清楚的「日曆」按鈕。 |
| UPDATE-ISSUE-025 | U-028 / U-029 | 背景補齊 / 技術指標跳過判斷 | 背景 TPEX 補齊狀態顯示 `技術指標: running`，但每日股價與技術指標已同日，應先檢查並跳過，不應強制全量或 250 天回看。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | UI 背景任務移除 `--technical-force-all`；腳本先比對 latest date，已最新時寫入 skipped，落後時才用 120 天 warmup。 |
| UPDATE-ISSUE-026 | U-024 | SQLite 檢視 / 券商分點篩選 | `broker_flows` 的券商分點名稱難記，純文字輸入不利驗證。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：選 `broker_flows` 後，券商分點旁按「展開」可看到下拉清單。 |
| UPDATE-ISSUE-027 | U-024 | SQLite 檢視 / 日期篩選 UX | 日期預設不應先填；日曆打開需跳今天；清除按鈕過多；單一日期要有今日按鈕；區間旁一個清除要同時清掉單一與區間；日曆圖示與日期格顯示不清。 | 通過 | 已修正 | 使用者已驗證 | 中 | 2026-06-18 人工驗證：SQLite 三個日期欄位預設空白，日期按「日曆」會跳到今天月份。 |
| UPDATE-ISSUE-028 | U-008 | 每日股價合併訊息 / TPEX 大表合併 | `daily_price_tpex/` 已有 0618 日檔，但合併 `stock_data_whole.csv` 完成提示仍停在 0617；根因是合併腳本只掃 `daily_price/`，未納入 TPEX 日檔目錄。 | 已修正待驗證 | 已修正 | 待使用者驗證 | 高 | `scripts/merge_daily_data.py` 改為同時掃 `daily_price/` 與 `daily_price_tpex/`；新增回歸測試覆蓋「既有大表 0617、TPEX 0618」時合併後最新日期應為 0618。 |

### 數據更新 Tab 收斂狀態

2026-06-18 使用者回報數據更新 Tab 功能面已完成人工驗證；本次補記的人工驗證項目包含每日股價 / 大盤 / 產業 / 券商分點結束日期日曆按鈕、SQLite 日期欄位預設空白、SQLite 日曆定位今天月份、`broker_flows` 券商分點下拉清單。

目前剩餘項目歸類為非功能或後續工程，不阻擋本輪功能驗收：

| Issue ID | 類型 | 後續事項 | 目前狀態 |
|---|---|---|---|
| UPDATE-ISSUE-013 | 效能評估 | 券商分點下載 / 合併是否可做受控並行，並確認 rate limit、retry 與站方阻擋風險。 | 已記錄，未修正，待排查 |
| UPDATE-ISSUE-014 | 效能評估 | 技術指標計算是否可拆成多核心計算與單點寫入，避免 SQLite / CSV 寫入競爭。 | 已記錄，未修正，待排查 |
| UPDATE-ISSUE-023 | 看板補強 | 「全部資料」看板缺月營收狀態卡，後續需補最新日期、筆數與狀態摘要。 | 已記錄，未修正，待處理 |

### 原始失敗紀錄

| ID | 來源功能 ID | 問題摘要 | 重現步驟 | 實際結果 | 預期結果 | 嚴重度 | 是否需要修復任務 |
|---|---|---|---|---|---|---|---|
| ISSUE-001 | U-002 / U-003 | 全部資料更新切換 subtab 後不再繼續 | 在「全部資料」點更新，尚未完成時切到其他 subtab。 | 更新不會繼續跑。 | 長任務應在背景繼續，UI 可切頁且完成後正確復原狀態。 | 高 | 是 |
| ISSUE-002 | U-008 | 每日股價合併中切 tab 後卡住 | 在「每日股價」執行合併 CSV，尚未完成時切到其他 tab；再執行匯出 CSV。 | 匯出 CSV 可完成，但合併仍顯示「合併中」，像是任務或 UI 狀態卡住。 | 合併任務應背景完成或明確失敗；按鈕與進度狀態必須復原。 | 高 | 是 |
| ISSUE-003 | U-022 | 券商分點合併中切 tab 後卡住 | 在「券商分點」執行合併，未結束時切到其他 tab。 | 會卡住，不能正常完成 / 復原。 | 合併任務應背景繼續，切頁不應中斷或卡住。 | 高 | 是 |
| ISSUE-004 | U-023 | 技術指標計算中切 tab 後中斷 | 在「技術指標」執行計算，未結束時切到其他 tab。 | 任務中斷，不會背景繼續。 | 技術指標計算應在背景繼續或提供明確取消 / 失敗狀態。 | 高 | 是 |
| ISSUE-005 | U-024 | SQLite 檢視日期篩選只能手打 | 在 SQL 資料檢視使用單一日期或區間篩選。 | 日期只能手動輸入。 | 希望提供日期選擇器 / 日曆 picker，減少輸入錯誤。 | 中 | 是 |
| ISSUE-006 | U-024 | SQLite 檢視出現簡體中文欄位 | 檢視 `daily_price` / `daily_prices` table。 | 「漲跌」欄位顯示簡體中文。 | 全系統使用者可見文字必須是繁體中文。 | 高 | 是 |
| ISSUE-007 | U-024 / U-023 | 漲跌欄位語意與顏色疑似錯誤，可能影響技術指標可信度 | 檢視 `daily_price` / `daily_prices` 與 `technical_indicators`。 | 「漲跌」欄位空白；「漲跌價差」都是正值且整欄綠色；方向似乎另在 `漲跌(+/-)` column；因此懷疑 technical indicators 是否以錯誤數據計算。 | 漲跌方向、價差正負號、顏色與技術指標輸入欄位必須語意一致且可追溯。 | 高 | 是 |
| ISSUE-008 | U-024 | SQLite 檢視所有欄位不能排序 | 在 SQLite 資料檢視點表格欄位。 | 所有欄位都不能排序。 | 希望能快速依指定欄位排序，同時不要大幅拖慢大表格。 | 中 | 是 |

## 本次未測 / 跳過原因

| 功能 ID | 原因 | 後續建議 |
|---|---|---|
|  |  |  |

## 備註

- 高風險資料操作（強制全量、清空、刪除正式紀錄）優先只驗證「取消 / 防呆 / 提示」流程；若要實際執行，需另行確認並先備份。
- 策略、回測、推薦、資金、倉位、風控與績效相關失敗，後續修復前需先做 Look-ahead bias 自查與金融數值邊界檢查。
- 本檔新增後不代表 Application Manual 功能行為有改變，因此不更新 Snapshot / Roadmap / Manual。
